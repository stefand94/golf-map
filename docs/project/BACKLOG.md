# Backlog

_Continue the `GOLF-nnn` sequence (archive's latest ≈ GOLF-98). Never reuse
an ID. Check for an existing item before adding a new one._

| ID | Name | Priority | Status | Deps | Notes |
| --- | --- | --- | --- | --- | --- |
| GOLF-99 | Trip sharing v1 — shareable read-only trip (map overview, day-by-day costs, itemised total) | P1 | SCOPED | — | Explicitly a "get a demo out" v1, to be redone properly later. A `#share=…` read-only view already exists — confirm how much of this is already live before building. Scoped in archive Phase 31 (line 1). |
| GOLF-100 | Group / per-person costing | P1 | PARTIAL? | GOLF-99 | Specify a group size → whole-trip + per-person cost, following the per-room/per-person model from GOLF-74. Toolbar "group size" control already exists — audit what's done vs outstanding. |
| GOLF-101 | Course imagery sourcing — written proposal | P2 | SCOPED | — | Copyright-safe options for course photos. Proposal only, not build. Archive Phase 31. |
| GOLF-103 | Hotel mapping — data source + visual treatment (options workshop, then build) | P2 | DISCOVERY | — | Extends GOLF-96 (Overpass pins + manual price). **Core need = coverage** (Overpass misses many real hotels); rough price = nice-to-have; live/date-based price = future (needs date selection). Does NOT block go-live. Price stays user-editable; keep Overpass as fallback. **Visual:** hotels are SECONDARY — owner does not want a map full of price stickers. Explore lightweight treatments: small colour-coded dots / density-heatmap style / toggleable layer / cluster counts. **Stay22 assessed — too hotel-forward, rejected as primary** (good as a UX benchmark only). Data candidates: **Travelpayouts/Hotellook** (free, aggregator coverage, indicative price, affiliate links = monetization — leading), **Google Places** (best coverage + ratings/photos, no price, ~£0 at low traffic then ~$30–35/1k, spend-cappable), **Amadeus** (structured price/availability — parked until date-selection milestone). **Decision deferred** — needs an options/scenarios workshop covering data source AND visual treatment together. Next concrete step: coverage bake-off across 6 real courses (city-adjacent + remote links). See chat 2026-09-07. |
| GOLF-104 | Backend / accounts / server-side persistence — direction paper | P2 | DISCOVERY | GOLF-103 direction settled | Move off localStorage-only (reopens DEC-004). Prerequisite for US expansion, cross-device trips, booking attribution, email. Big refactor. **Sequenced: post go-live, AND after the hotel/POI direction (GOLF-103) is clear** so the persistence model is designed around known data needs. Written proposal only for now: options (managed BaaS vs minimal API + DB), auth model, migration of existing localStorage trips, cost, impact on the "zero-backend static" constraints. |
| GOLF-105 | Basemap tile upgrade — raster style swap (keep Leaflet) | P2 | SCOPED | — | Current: plain **public OSM raster tiles** via Leaflet 1.9.4 (`js/map.js` line ~31; 2nd map in `js/trip-share.js` line ~212). Public OSM tile server is not a legitimate production basemap regardless of looks (R-8). Low-effort: swap the `L.tileLayer` URL to a clean, muted, pin-friendly style. Provider bake-off = **MapTiler vs Stadia** (both free tier, key restricted to live+dev domains — a public-by-design tile key, not a secret like ORS). ~1–2 hrs incl. attribution + retina/@2x + both maps. ~£0 at launch traffic, spend-cappable. Owner picks the look on the preview URL. Handover: `HANDOVER-GOLF-105.md`. Explicitly NOT the maptiler.com vector demo (= GOLF-106). |
| GOLF-106 | Vector basemap / MapLibre GL migration | P3 | IDEA | GOLF-105 | The smooth continuous-zoom / rotate / crisp-label look = vector tiles, which need the **MapLibre GL** engine (Leaflet can't render them). Full port re-implements markers, clustering, the bespoke rail-line geometry, popups, bounds-fit, mobile list/map toggle, and the share-view map — all 9 `L.`-using JS files — multi-day refactor + regression risk. **Legit long-term goal, not overkill**, but post go-live; revisit when the map carries heavier load (US data, toggleable layers) — likely around/after GOLF-104. `maplibre-gl-leaflet` shim is a lower-risk middle path with limited payoff. |
| GOLF-107 | Pre-go-live UX refinement pass | P1 | DISCOVERY | — | Owner compiling an issue list. Each item triaged **code-fix vs hosting/Cloudflare** — rule of thumb: reproduces on localhost / opening the raw HTML = code; only happens on the deployed URL = hosting. Blocks go-live by owner's own ranking. Bring the list here for triage into tickets. |
| GOLF-35 | Custom domain / production hosting consolidation + hidden dev instance | P2 | READY | Phase B blocked on user buying domain | Decisions locked (DEC-006). Handover: `HANDOVER-GOLF-35.md`. Phase A (dev password gate, noindex, GitHub Pages retire prep) needs no domain — ship first. **Handover process:** start a fresh Claude Code session (not a BA/PM one), point it at `HANDOVER-GOLF-35.md`, do Phase A only, on a branch (not `main`) so the preview URL can be tested before merge. Owner does dashboard task S3 (`DEV_PASSWORD`, Preview scope) when agent says code is ready. Verify acceptance criteria before REVIEW/COMPLETE. |
| GOLF-102 | ORS Worker abuse hardening — CORS allowlist + rate limiting | P2 | READY | GOLF-35 Phase B for rate limiting | Part 1 (CORS allowlist) ships with GOLF-35 Phase A. Part 2 (rate limit rule) needs Worker on the custom domain. In `HANDOVER-GOLF-35.md`. |
| — | Green-fee data for Scotland / Ireland / South Africa / Wales / London | P2 | QUEUED | GOLF-98 England batch merged | Continuation of GOLF-98 per-nation. |

## Recently completed (one line each — prune quarterly)

- GOLF-97 — banded green-fee schema (`fee:{…}`) — live
- GOLF-96 — "Add a stay" map hotel picker — app live, Worker redeploy pending
- GOLF-95 — prompt-based reorder suggestion (no silent reorder)
- GOLF-94 — nation pills moved to top; auto 1-day-per-course scheduling
- GOLF-83/83b/83c — heritage POIs wiki-first + wineries
- Explore mode retired; pin redesign; Nearby/place-search consolidation
- GOLF-85 — Scotland Top 100 broadening; Ireland/SA Top 100 broadening
- GOLF-70/71/72–76 — script modularisation + design-system pass
