# Backlog

_Continue the `GOLF-nnn` sequence (archive's latest ≈ GOLF-98). Never reuse
an ID. Check for an existing item before adding a new one._

_Minor defects / visual regressions live in `BUGS.md` (GOLF-123+)._

| ID | Name | Priority | Status | Deps | Notes |
| --- | --- | --- | --- | --- | --- |
| GOLF-99 | Trip sharing v1 — shareable read-only trip (map overview, day-by-day costs, itemised total) | P1 | SCOPED | — | Explicitly a "get a demo out" v1, to be redone properly later. A `#share=…` read-only view already exists — confirm how much of this is already live before building. Scoped in archive Phase 31 (line 1). |
| GOLF-100 | Group / per-person costing | P1 | DONE | GOLF-99 | Per-person "£X pp" figure under every Costs-tab amount (line rows, category headers, fuel, grand total), shown when group size > 1, driven by the toolbar group-size control. GOLF-74 per-room model resolved from code: GOLF-91 replaced it with per-person-per-night × group size, divides cleanly everywhere. `js/trip-ui.js` `costPP()` / `costGroupHTML()`, `js/trip-share.js` for the shared view. Merged to `main` (2026-09-08). |
| GOLF-101 | Course imagery sourcing — written proposal | P2 | SCOPED | — | Copyright-safe options for course photos. Proposal only, not build. Archive Phase 31. |
| GOLF-103 | Hotel mapping — data source + visual treatment (options workshop, then build) | P2 | DISCOVERY | — | Extends GOLF-96 (Overpass pins + manual price). **Core need = coverage** (Overpass misses many real hotels); rough price = nice-to-have; live/date-based price = future (needs date selection). Does NOT block go-live. Price stays user-editable; keep Overpass as fallback. **Visual:** hotels are SECONDARY — owner does not want a map full of price stickers. Explore lightweight treatments: small colour-coded dots / density-heatmap style / toggleable layer / cluster counts. **Stay22 assessed — too hotel-forward, rejected as primary** (good as a UX benchmark only). Data candidates: **Travelpayouts/Hotellook** (free, aggregator coverage, indicative price, affiliate links = monetization — leading), **Google Places** (best coverage + ratings/photos, no price, ~£0 at low traffic then ~$30–35/1k, spend-cappable), **Amadeus** (structured price/availability — parked until date-selection milestone). **Decision deferred** — needs an options/scenarios workshop covering data source AND visual treatment together. Next concrete step: coverage bake-off across 6 real courses (city-adjacent + remote links). See chat 2026-09-07. |
| GOLF-104 | Backend / accounts / server-side persistence — direction paper | P2 | DISCOVERY | GOLF-103 direction settled | Move off localStorage-only (reopens DEC-004). Prerequisite for US expansion, cross-device trips, booking attribution, email. Big refactor. **Sequenced: post go-live, AND after the hotel/POI direction (GOLF-103) is clear** so the persistence model is designed around known data needs. Written proposal only for now: options (managed BaaS vs minimal API + DB), auth model, migration of existing localStorage trips, cost, impact on the "zero-backend static" constraints. |
| GOLF-105 | Basemap tile upgrade — raster style swap (keep Leaflet) | P2 | READY | — | Current: plain **public OSM raster tiles** via Leaflet 1.9.4 (`js/map.js` line ~31; 2nd map in `js/trip-share.js` line ~212). Public OSM tile server is not a legitimate production basemap regardless of looks (R-8). Low-effort: swap the `L.tileLayer` URL to a clean, muted, pin-friendly style. Provider bake-off = **MapTiler vs Stadia** (both free tier, key restricted to live+dev domains — a public-by-design tile key, not a secret like ORS). ~1–2 hrs incl. attribution + retina/@2x + both maps. ~£0 at launch traffic, spend-cappable. Owner picks the look on the preview URL. Handover: `HANDOVER-GOLF-105.md`. Explicitly NOT the maptiler.com vector demo (= GOLF-106). |
| GOLF-106 | Vector basemap / MapLibre GL migration | P3 | IDEA | GOLF-105 | The smooth continuous-zoom / rotate / crisp-label look = vector tiles, which need the **MapLibre GL** engine (Leaflet can't render them). Full port re-implements markers, clustering, the bespoke rail-line geometry, popups, bounds-fit, mobile list/map toggle, and the share-view map — all 9 `L.`-using JS files — multi-day refactor + regression risk. **Legit long-term goal, not overkill**, but post go-live; revisit when the map carries heavier load (US data, toggleable layers) — likely around/after GOLF-104. `maplibre-gl-leaflet` shim is a lower-risk middle path with limited payoff. |
| GOLF-107 | Pre-go-live UX refinement pass (umbrella) | P1 | SCOPED | — | Owner's bug list 2026-09-07 triaged into GOLF-108…117 + a GOLF-100 addition. All code/UX, no hosting. Full scope + acceptance criteria: `GOLF-107-uxpass.md`. |
| GOLF-108 | Course pins suppressed once a trip exists (map visibility, Discover + Itinerary) | P1 | READY | — | Decision made: build a "show nearby courses" toggle, default ON. Root cause: `js/explore.js` render() ~L455 early-return. Merges owner items 1 + 11. Blocker. `GOLF-107-uxpass.md`. |
| GOLF-109 | Multi-course venues — all courses show on map (Turnberry Ailsa + KRTB) | P1 | READY | GOLF-108 | Likely a symptom of GOLF-108; if not, inspect `passes` filter vs sidebar list. Blocker. |
| GOLF-110 | Hide nearest-railway-station feature behind a flag (dormant, not deleted) | P2 | DONE | — | DEC-008. `RAIL_FEATURE=false` in `js/map.js`: rail polylines/station dots/labels never built, `restyleRail()`/`zoomend` short-circuit, `drawLink()` no-ops, "By rail" popup row gated, `js/explore.js` toggle wiring gated. Data + functions kept; flip flag → full restore. Merged to `main`. |
| GOLF-111 | Course marker redesign — golf ball on a tee | P2 | DONE | — | `golfPinSVG()` (`js/util.js`) rebuilt from the owner's reference PNG: flat inline SVG, white ball + dimple hints, coloured tee, dark outline, subtle drop-shadow, no gradients. `pinFor()` (`js/map.js`) anchors at the tee tip and passes `{ranked, tint}`; ranked keeps the 30-vs-22 size step + a gold halo; `pinStateTint()` maps in-trip → blue / played → green / want → amber tee (default red), refreshed live by `render()`'s existing `setIcon` loop. `.mcluster` badge restyled white/dark to match. Merged to `main` (2026-09-08). |
| GOLF-112 | City search — click result to focus map (not only add stop) | P2 | DONE | — | Place-name link (`.tb-unified-place-focus`) flies the map + drops a temp 📍 marker (cleared on next search / manual pan-zoom / Escape), no trip stop; "Add to trip" stays a separate button. Click also re-scopes Discover "Nearby" to the place (`tbPlaceAnchor`). geocode-403 degradation untouched. `js/map.js` `tbFocusPlaceOnMap()`, `js/trip-add.js`, `js/trip-ui.js`. Merged to `main` (2026-09-08). |
| GOLF-113 | "Discover by Region" — region list must match selected nation | P1 | DONE | — | `tbRegionOptionsHTML()`/`tbRegionsForNation()` in `js/trip-ui.js` filter regions to those with ≥1 course in `state.nation`; no nation → `<optgroup>` by nation. Switching nation drops a now-invalid `tbRegion`. Merged to `main`. |
| GOLF-114 | Header + nation-pill layout — spacing, full-width, even/dynamic distribution | P1 | DONE | — | `.nation-pills` now `display:grid; grid-template-columns:repeat(var(--nation-count),1fr)` — `--nation-count` set inline from `NATIONS.length` by `tbNationPillsHTML()`, so a 4th/5th nation redistributes with no CSS change. `--sp-6` top padding = header→pills breathing room, `--sp-5` sides match the gutter, pills `min-height:var(--tap)` (44px), long labels wrap and stay equal height. Verified at 360px. Merged to `main`. |
| GOLF-115 | Long trip name gets clipped | P1 | DONE | — | Trip-menu summary wraps the name in a `.tb-drop-label` span that ellipsises inside the fixed toolbar cell (row never grows) with the full name as a `title` tooltip; share payload now carries `nm` and the shared view shows the full name. Merged to `main`. |
| GOLF-116 | Costs tab — consolidate hotel stays into one line (`Hotel A (R300/night × 2) R600`) | P1 | DONE | — | `tripCostLineItems()` (`js/trip-ui.js`) groups hotel night-items by `stayId` (fallback `name + lat/lng` 3dp) into one row `£X/night × N nights`. Grand total unchanged (sum of the same per-night values). Merged to `main` (2026-09-08). |
| GOLF-117 | Research spike — ferry legs (Scottish islands, Islay↔Ireland) | P3 | COMPLETE | — | Done 2026-09-08 — `GOLF-117-ferry-spike.md`. Findings: ORS `driving-car` already crosses `route=ferry` ways (verified live: Islay/Mull/Cairnryan yes, Arran no) but inconsistently and with no timetable awareness; ferry legs are detectable by adding `extra_info:["waytypes"]` (code 9) to the Worker — small change; no free CalMac API (TNDS XML only); Kintyre Express (Islay↔Ireland) is passenger-only + seasonal; Port Ellen shut 2026–29. Recommendation: flag-only v1 → GOLF-118. |
| GOLF-118 | Flag ferry legs + split ferry/drive time in the itinerary | P3 | READY | — | Approved by owner 2026-09-08. Worker adds `extra_info:["waytypes"]`, returns `hasFerry` + `ferryMinutes` + `ferryMiles` (backwards compatible). Itinerary drive-leg rows with a ferry show a **"⛴ This route has a ferry"** tag and split the leg time into *"Xh Ym driving + ~Zm ferry"*. Operator link from a curated `data/ferries.js` is nice-to-have, not required for v1. NO timetables/fares (parked until date selection). Known: ORS sometimes skips a ferry and routes long — `hasFerry:false`, acceptable. Scope: `GOLF-117-ferry-spike.md` §4. Handover: `HANDOVER-GOLF-118.md`. Post-go-live. |
| GOLF-119 | DotGolf club-finder API — geographic coverage audit | P2 | READY | — | XS desk-research ticket (owner ask 2026-09-08). The `GetClubsByName`/`GetClubDetails`/`FindClubs` "DotGolf" white-label API is confirmed live behind England Golf, Scottish Golf, Wales Golf, Golf Ireland and SA (`handicaps.co.za`) — see `scripts/README.md`. **Owner notes NZ + Australia also appear to run DotGolf — check `golf.co.nz` / `golf.org.au` (GolfLink) for the same endpoint shape.** Deliverable: a short written note listing which national bodies expose this API (and the base URL + whether a bulk `FindClubs` list works vs name-only search), plus a one-line verdict per candidate future geography (US = GHIN/USGA, different system; continental Europe = ?). Feeds GOLF-121 and any future expansion. No code beyond a throwaway probe script. |
| GOLF-120 | Green-fee schema v2 — granular seasonal / twilight / time-band fee data | P1 | SCOPED | — | Owner 2026-09-08: spot-checks show current `fee:{wd,we}` values inaccurate. Want a richer source structure capturing high/shoulder/winter season, weekday/weekend, twilight, time bands, 9-hole, guest-of-member, cart — displayed figure derived as "highest applicable". Forward-looking toward per-tee-time planning/booking. **Big job:** schema design (spec + owner sign-off) FIRST, then re-research every course incl. the England Top 100 already migrated. **Gates the remaining-nations green-fee work — pause GOLF-98 continuation until v2 schema is locked.** Research via the established fetch-once → JSON → manual-merge pattern (NOT scraping — BRS Golf ToS, see GOLF-98). To be scoped 2026-09-09. |
| GOLF-121 | Course completeness — bulk DotGolf course pull, per country (SA first) + missing sister / 36-hole-club courses | P2 | IDEA | GOLF-119 | Owner 2026-09-08: two linked goals. (1) **Bulk pull every course DotGolf lists for a country** via the established fetch-once → JSON → manual-merge pattern, not just the hand-curated Top 100 — **start with South Africa** (owner's main testers are there), then expand nation-by-nation (Ireland, Scotland, Wales, England, NZ/Aus if GOLF-119 confirms coverage) **as token budget allows**. (2) Clubs with 2+ courses often only carry one course row today — DotGolf finds *clubs* but its `NoOfHoles` is documented-unreliable and it's club- not course-level, so 36-hole/sister-course completeness still needs a Top100 / club-website cross-check. NOT the same as GOLF-109 (render bug for courses already in the data). Bulk pull returns no green fees — those come via GOLF-120. To be scoped 2026-09-09 alongside GOLF-120. |
| GOLF-122 | Stale content after deploy — service-worker update lag | P1 | REVIEW | — | Fixed on branch `golf-122-sw-networkfirst`: `sw.js` v6 — navigation requests (`req.mode === 'navigate'`) are network-first (try network → `stripRedirect` cleanup → refresh cache → return; offline falls back to `caches.match(req)` then the canonical `./london-golf-map-v5_1` shell). Everything else stays cache-first. Redirect-cleanup extracted to a shared `stripRedirect()` helper reused by both `install` and `fetch`. `check_js` + `test_data` pass; local load clean. **Deploy-specific acceptance criteria (single-reload freshness, offline, fresh-profile first load) still to be verified on the Cloudflare preview URL for the branch.** Handover: `HANDOVER-GOLF-122.md`. |
| GOLF-35 | Custom domain / production hosting consolidation + hidden dev instance | P2 | READY | Phase B blocked on user buying domain | Decisions locked (DEC-006). Handover: `HANDOVER-GOLF-35.md`. Phase A (dev password gate, noindex, GitHub Pages retire prep) needs no domain — ship first. **Handover process:** start a fresh Claude Code session (not a BA/PM one), point it at `HANDOVER-GOLF-35.md`, do Phase A only, on a branch (not `main`) so the preview URL can be tested before merge. Owner does dashboard task S3 (`DEV_PASSWORD`, Preview scope) when agent says code is ready. Verify acceptance criteria before REVIEW/COMPLETE. |
| GOLF-102 | ORS Worker abuse hardening — CORS allowlist + rate limiting | P2 | READY | GOLF-35 Phase B for rate limiting | Part 1 (CORS allowlist) ships with GOLF-35 Phase A. Part 2 (rate limit rule) needs Worker on the custom domain. In `HANDOVER-GOLF-35.md`. |
| — | Green-fee data for Scotland / Ireland / South Africa / Wales / London | P2 | QUEUED | GOLF-98 England batch merged | Continuation of GOLF-98 per-nation. |

## Recently completed (one line each — prune quarterly)

- GOLF-117 — ferry-leg research spike: ORS crosses ferries already (inconsistently), flag-only v1 recommended → GOLF-118
- GOLF-114 — nation-pill row: equal-width grid keyed off `NATIONS.length` + header spacing
- GOLF-110 — nearest-railway-station feature hidden behind `RAIL_FEATURE` flag (dormant)
- GOLF-113 — "Discover by Region" list now scoped to the selected nation
- GOLF-115 — long trip names no longer clipped (ellipsis + title; shared view shows full name)
- GOLF-97 — banded green-fee schema (`fee:{…}`) — live
- GOLF-96 — "Add a stay" map hotel picker — app live, Worker redeploy pending
- GOLF-95 — prompt-based reorder suggestion (no silent reorder)
- GOLF-94 — nation pills moved to top; auto 1-day-per-course scheduling
- GOLF-83/83b/83c — heritage POIs wiki-first + wineries
- Explore mode retired; pin redesign; Nearby/place-search consolidation
- GOLF-85 — Scotland Top 100 broadening; Ireland/SA Top 100 broadening
- GOLF-70/71/72–76 — script modularisation + design-system pass
