# Backlog

_Continue the `GOLF-nnn` sequence (archive's latest ≈ GOLF-98). Never reuse
an ID. Check for an existing item before adding a new one._

_Minor defects / visual regressions live in `BUGS.md` (GOLF-123+)._

| ID | Name | Priority | Status | Deps | Notes |
| --- | --- | --- | --- | --- | --- |
| GOLF-153 | "Detailed mode" — a timed, itemised day plan (flights, tee times, tours, drives, prices) | P1 | **PROPOSED — owner-initiated, post-go-live roadmap, not a blocker** | GOLF-148 | **2026-09-19, owner:** "I wonder if we could have a detailed mode. This would align a bit more closely with how I have planned some trips in the past... It becomes necessary once trips are being finalised, so not a blocker for go live but definitely a key item on the roadmap." Owner's own worked example: *"Day 1: Depart 9:40 from LCY (BA942) / Arrive Inverness Airport 10:55 — Drive to Cabot Highlands 5 mins / 12:24 Tee off Cabot Highlands (350 GBP) / Drive to Dufftown — Stay at Dufftown Inn (1 night, 50GBP/pp) / Day 2: 10:00 GlenAllachie Distillery Tour (20GBP) 10 min drive / 13:00 Balvenie Tour (15GBP) 20 min drive"*. **What this actually is:** today's day card is an unordered *set* of items with a computed drive between located stops; detailed mode is the same data rendered as a **clock-ordered timeline**, plus three fields the model doesn't have yet — a **time** per item, a **duration** per item, and **item types the model can't express** (a flight; a tour/activity with a booking time and price; possibly a meal). The existing `items[]` array is already the ordered source of truth, so this is additive, not a rewrite: add optional `time`/`durationMins` to an item, add `type:'travel'` (flight/train/ferry) and fold tours into the existing `poi` type with a time and price, then a second renderer. **Design questions to settle before building:** (1) is time *entered* or *computed* — i.e. does a tee time at 12:24 push everything after it, with drive times filling the gaps automatically, or is it a free-text plan the visitor maintains? (Computed is the stronger product and the reason to own this rather than export to a doc, but it needs the drive legs to be trustworthy, which GOLF-149 only just made true.) (2) Is it a **mode** (toggle on the Itinerary tab) or the **final state** of every trip once dates exist? (3) Flights: typed by hand, or looked up? Hand-typed first — no flight API is in scope. (4) Does it need a print/export view, since "finalising" usually ends in a PDF or a message to the group? **Deliberately not started:** go-live scope is Discover → Itinerary → Costs working well; this is the next thing after. |
| GOLF-152 | Day reordering is clunky — drag-and-drop doesn't auto-scroll, no direct "move to day N" | P2 | **PROPOSED — owner-initiated** | — | **2026-09-19, owner:** "is there a better way to do day reordering? The drag and drop is a bit clunky and if I'm dragging a day from 6 to 1 it doesn't automatically scroll up, I have to do it in stages." Confirmed by inspection: day drag uses the HTML5 drag API (`tbDayDragSet`/`tbDayMoveTo` in `js/trip-model.js`) with no edge-auto-scroll, so a long trip can only be reordered a screen at a time, and on touch the HTML5 drag API is unreliable anyway. **Three candidate fixes, cheapest first:** (a) **auto-scroll the pane** while a drag is near its top/bottom edge — smallest change, fixes the stated complaint, leaves everything else as is; (b) **"Move to…" in the day's ⋯ menu** → pick a position (or Move up / Move down / Move to start / Move to end) — keyboard- and touch-accessible, no dragging at all, and the obvious answer for 6→1; (c) replace the HTML5 drag with pointer-events dragging, which also fixes touch. **Recommendation: (b) then (a).** (b) is the one that makes a 6→1 move a single action rather than a better-behaved long drag, and it's the accessible path; (a) then makes short drags feel right. (c) is a bigger rewrite and only worth it if reordering by drag on a phone turns out to matter. |
| GOLF-151 | Map place names unreadable on retina screens (`detectRetina` halving every label) | P2 | **COMPLETE** | — | **2026-09-19, owner:** "as you zoom in town names get smaller and smaller. It's very difficult to see the names of places. This gets particularly annoying in more rural areas." **Cause:** `esriBaseLayers()` set `detectRetina:true` on every Esri layer. Esri's keyless `arcgisonline` endpoints serve no `@2x` tiles, so Leaflet's fallback behaviour applies — it requests the **next zoom level down** and draws those tiles at half size. The imagery genuinely looks sharper that way, but every *label* is baked into the raster, so all text rendered at half physical size, and the deeper zoom level also means sparser, smaller type. Worst in rural areas, where a hamlet's name is the only thing on the tile. **Fix:** `detectRetina` now only on `World_Imagery` (photography, no text); the street basemap and the satellite reference/label overlays render at their true zoom. Verified at Dufftown z13 — tiles now fetched at z13, not z14, and labels are twice the size. Same factory serves the shared-trip map, so that's fixed too. |
| GOLF-150 | Pre-go-live UI refinement pass #2 (umbrella) | P1 | **COMPLETE — batches 1 and 2 on `main` (`32e7545`); Worker redeployed by hand 2026-09-19, S1 `layers=coarse` verified live** | — | **2026-09-19, owner-initiated:** "There are a number of UI refinements and changes that need to be done as well now, I'm not sure if you want me to try and scope all of these out or if you can pick up on some of them." Agreed approach: **both, in parallel, because they find different defects.** The owner writes up what actually irritates them in use — only they know which things grate over repeated real use, and that list is not derivable by inspection. Separately a structured review pass runs over the app and writes findings to a `GOLF-150-ui-review.md`, the way GOLF-133 did (which produced GOLF-134…140 and found things the owner hadn't articulated). The two lists get merged and triaged into numbered tickets rather than fixed ad hoc. **Deliberately sequenced after GOLF-149** — reviewing UI while driving routes were returning a Cloudflare 1101 would have generated a page of bogus findings about the routing UI. Routing is now verified live, so this is unblocked. ~~Next action is the owner's: supply the list.~~ **2026-09-19 update:** owner's list received and merged with the review pass → `docs/project/GOLF-150-ui-review.md`. Batch 1 shipped (trip name as headline, icon share/clear/filters, segmented group-size, toolbar overflow, no name prompt on New trip) plus 3 defects found (search-result layout shift creating days by mis-click, leaked Explore strip, reorder-card padding). ~~Next action: owner triages the proposals.~~ **2026-09-19 update 2:** owner approved every proposal ("100% spot on"); all built as batch 2 on branch `golf-150-ui` (see the doc's "Batch 2" section). **Next action:** owner reviews the branch's Cloudflare preview URL → merge to `main` → Stefan manually redeploys the Worker (S1 `layers=coarse` is a Worker change; a push doesn't deploy it). GOLF-148 step 3 (POI UI) follows the merge. |
| GOLF-149 | Driving routing dead in production — `json()` called with `request` in the status slot | P0 | **COMPLETE — verified live** | GOLF-129 (cause) | **2026-09-19.** `54f8853`. Owner: "the driving routing seems to have gone." **Every driving route in the app had been failing, and had been since GOLF-129.** `handleRoute`'s *success* path called `json({...}, request)` — but the helper is `json(obj, status = 200, request)`, so the `Request` object landed in the **status** position, `new Response(..., {status: Request})` threw a RangeError, and Cloudflare surfaced it as **error 1101**. Fixed to `json({...}, 200, request)`. **Why it hid for so long:** the error paths in the same function all pass an explicit numeric status, so they were correct — the function only broke when it *succeeded*. Every other Worker mode (`heritage-pois`, `hotels`, `hotelsViewport`, `pois`, `geocode`) was healthy, so the Worker looked fine from the outside and nothing logged an obvious failure. Introduced by `8803a68`, the GOLF-129 CORS work, which threaded `request` through every `json()` call site and got this one call's argument order wrong; located with `git log -L 467,473:scripts/cloudflare-worker/ors-proxy.js`. **Explicitly not caused by GOLF-146/147** — none of those commits touch `js/ors.js`, `js/trip-route.js` or `js/trip-geo.js`; checked before reporting, because the regression surfaced in the same session as the performance work. Reproduced offline first with a stubbed-ORS harness rather than guessing against production. **Regression cover:** `scripts/test_worker_cache.mjs` 28 → 32, now asserting a successful route returns **200 and not 1101**, with plausible duration/distance, `simplifyRoute()`-capped geometry as `[lat,lng]` pairs, and CORS headers present — the stub feeds 5000 coordinates specifically so the simplifier is exercised. **Verified in production** after the owner's manual Worker redeploy (Git-integration deploys have a history of not taking effect — GOLF-142): Edinburgh→Glasgow 62.37 min / 45.95 mi, Edinburgh→Leith 9.59 min / 2.40 mi, St Andrews→Edinburgh 79.28 min / 52.39 mi, all with route geometry. **Lesson worth keeping: a positional-argument helper with an optional middle parameter is a trap** — `json(obj, request)` is a completely reasonable-looking call that type-checks nowhere in a plain-JS Worker. If a third caller shape ever appears, make it an options object. |
| GOLF-148 | Notable POIs along a route — pre-baked dataset + ranking + clickable pins | P1 | **IN PROGRESS — step 3 (runtime + UI) built and on `main`; waiting on the merged `data/pois-*.js` files (step 2)** | GOLF-146, GOLF-147 | **2026-09-19, owner-initiated:** "You should be able to see interesting POIs along your route whether they are whisky distilleries, national parks, castles, beaches or anything else worth stopping for." Scope agreed with owner: show the **5 most notable** things along a route, a **"show more POIs"** button for a deeper tier, **ranking**, and **clickable pins that add to the itinerary exactly like GOLF-145's hotels**. Owner also asked whether this needs a database — **answer: no, it needs a pre-computed build-time dataset**, and the decision hinges on a distinction worth recording: **hotels are dense and change constantly and you can pan anywhere, so they must stay live+cached (GOLF-146/147); notable POIs are sparse and stable (castles don't move) and the product only ever shows the top handful, so the entire useful dataset is small enough to ship statically.** Sizing: a record (name, lat, lng, category, score, wikidata id) is ~100 bytes, so even 10k POIs is ~1MB raw / ~180KB gzipped, against the ~180KB gzipped the app already ships in `data/*.js` — one more course file, lazy-loaded so first paint doesn't grow. This keeps CLAUDE.md's "zero runtime API calls for course data" pattern, removes Overpass from the runtime path entirely (no 8–50s latency, no 504s, no DEC-016 fair-use exposure), computes ranking once offline instead of per request, and makes "show more" a deeper slice of an in-memory array rather than a second slow round trip — which is the strongest single argument for pre-baking given the agreed scope. A real DB would only be needed if POIs were user-contributed, live-updated, or too big to ship; none apply. **Built so far:** `scripts/fetch_pois.py` — fetch-once → JSON intermediate (`scripts/output/pois_raw.json`), never touches `data/*.js`, per CLAUDE.md. 6 regions (England/Scotland/Wales/Ireland/N. Ireland/South Africa) × 7 themed category groups, split per group because one query for everything times out and a failed group can be re-run alone. **Ranking = Wikidata sitelink count** (how many language Wikipedias cover it — Edinburgh Castle ~60, a minor listed building 1) **plus a per-category baseline** (`CATEGORY_BASE`), because sitelinks alone would rank every gallery above every national park; the baseline is where the product opinion lives and is meant to be tuned. Groups are **gated** (kept only with a wikipedia/wikidata tag) for museum/attraction/viewpoint/peak, which are applied to enormous numbers of trivial objects, and **ungated** for distillery/national park/beach/lighthouse/nature reserve, where the tag itself is the recommendation — a working distillery with no Wikipedia article is exactly what this feature should surface. Probe run verified: 177 Scottish distilleries + 56 breweries. **Open/next:** full fetch running; then decide per-leg vs whole-trip suggestion scoping and the "show more" tier size, then the runtime (point-to-polyline distance with a bbox prefilter, top-N by score) and the add-to-trip popup reusing GOLF-145's pattern. **Owner asked 2026-09-19 where the "Show POIs" button is — it does not exist yet and is correct to be missing: this ticket is still at the dataset stage and no runtime/UI has been written.** The visible deliverables (the button, the pins, add-to-trip) are all still to come. **State of the fetch as of 2026-09-19 18:30, for whoever picks this up:** `scripts/fetch_pois.py` was running locally against Overpass and had completed **England/notable — 7,714 POIs kept**, writing to `scripts/output/pois_raw.json` (~2MB, **gitignored**, so it exists only on the owner's machine and will not appear in a fresh clone). It was mid-England/open, backing off through Overpass 429/504s. **The script is idempotent and safe to simply re-run from scratch** — it is a fetch-once job with per-region dumps, exponential backoff, a two-pass retry over failing tiles, and a loud `HOLES` report plus exit code 2 if any tile never recovers, so an interrupted run loses time, not correctness. If the machine was restarted or the run was killed, re-run `python3 scripts/fetch_pois.py` and expect it to take a while; Overpass rate-limits hard and the pauses are deliberate (DEC-016 fair use). Merging `pois_raw.json` into a lazy-loaded `data/pois-*.js` is the next step after the fetch completes, and has not been started. **2026-09-19 — step 3 built (`js/poi.js`), replaces the old live-Overpass "Show POI's" link.** Each Itinerary day gets a "Things to see" link beside "Staying near": the 5 best-scored places within 5 mi of that day's drive (previous day's last stop → this day's stops, along the ORS route line, straight line as fallback) or ~9 mi of a stop; "Show more" goes to 20; chips filter Heritage / Nature / Coast / Culture / Drink; ＋ adds to the day as a stop with an Undo toast; tapping a name opens its map pin, whose popup adds to any day. Ranking is isolated in `poiRank()` (score, then distance from route, then name). **Data contract for step 2:** `data/pois-categories.js` → `const POI_CATEGORIES=[{label,group}]` (group ∈ heritage/nature/coast/culture/drink); `data/pois-<region>.js` → `window.P_<REGION>=[[name,catIdx,lat,lng,score],…]` for england/scotland/wales/ireland/southafrica (NI goes in ireland). Both are lazy-loaded on first use; region files are network-first in `sw.js` and deliberately not precached. Until they exist the list says "Sights aren't available for this area yet." The Worker's old `heritage-pois` mode is now unused. **South Africa:** owner's call to include it — coverage is thinner than GB in both OSM and Wikidata and the region will feel sparser; owner's view is that what *is* there will be higher quality, and that's accepted. |
| GOLF-147 | Hotel layer is slow — fix the upstream mirror, hedge the fallback, cache client-side | P1 | **COMPLETE** | GOLF-142, GOLF-144, GOLF-146 | **2026-09-19.** Owner: "The hotels show. I'm happy with that. There needs to be some performance improvements though, it's very slow." `50e711b`. **Root cause was a misdiagnosis recorded in the Worker's own comments.** `OVERPASS_URLS` had `maps.mail.ru` first because overpass-api.de "blocks Cloudflare Worker egress IPs" — calls from inside the Worker always failed, the same call from a laptop always worked. That was wrong: **it is the User-Agent.** overpass-api.de answers **406 Not Acceptable** to a request that sends none, and Workers' `fetch()` sends none by default — which is also why a plain `curl`/urllib call reproduces the identical 406 from an ordinary machine, and why local `wrangler dev` testing hit 406 during GOLF-146. Never an IP problem. Measured over four real viewports (St Andrews, Edinburgh, Sandwich, Cape Town): **overpass-api.de median 2.85s / max 9.73s vs maps.mail.ru median 13.38s / max 47.75s** — so the Worker had been pinned to the slow mirror for the whole life of the feature. Sending `OVERPASS_UA` and promoting overpass-api.de is the bulk of the win. Note `scripts/fetch_sa_golf_overpass.py` had the correct order + UA all along; only the Worker was out of step. **Query shapes deliberately untouched:** benchmarking found the same query against the same mirror ranging 8s → 50s timeout, so run-to-run variance dwarfs any difference between regex and exact-value forms — an initial "regex is 3× slower" reading was noise and was retracted rather than shipped. **Hedging:** three byte-identical copies of a serial retry loop replaced by one `overpassFetch()`. The old loop gave mirror 1 unlimited time and only tried mirror 2 once it failed outright, so it had no answer to the dominant case — "mirror 1 is simply very slow". Now the primary goes out immediately and the fallback is raced *alongside* it only if the primary hasn't answered in 4s; first usable response wins, losers aborted, 25s per-attempt ceiling. A second request therefore only happens when the first is already slow, keeping steady state at one call per miss (DEC-016). A 200 carrying a `remark` and no `elements` counts as a failure so the other mirror can win rather than an empty answer being cached. **Client cache:** `js/hotel-layer.js` keeps an in-memory viewport cache on the same 0.01° grid the Worker snaps to, so panning back costs **no network at all** (measured 8ms to redraw). Sharing the grid also means every client viewing one town sends a byte-identical bbox and they collide on a single edge-cache entry instead of each minting their own. A viewport fully inside an already-fetched one is reused, making **zoom-in free** — except when the cached set hit Overpass's 80-result cap, where reuse would silently show *fewer* hotels the further you zoom in; those are re-fetched. In-memory only, not localStorage: browsing state, not trip data. Responses are cached *before* the draw-staleness guards, since a response can be correct for its cell yet not worth drawing right now. **Also fixed:** `js/hotel-layer.js` and `js/trip-share.js` were in the page's `<script>` list but never in `sw.js`'s `PRECACHE_URLS` — re-fetched on every load and absent offline, a months-old oversight invisible by nature. Added, and `scripts/check_js.js` now asserts the two lists agree so a new module can't repeat it. Worker tests **19 → 28** (mirror order, User-Agent present, no fan-out on a healthy primary, fast-fail hedges immediately, slow primary hedges and the fallback answer wins, clean 502 when both are down). Verified in-browser: sub-cell pan 0 calls, zoom-in 0 calls, new area 1 call, pan-back 0 calls / 8ms, saturated entry correctly re-fetched. |
| GOLF-146 | Edge-cache the Worker's Overpass modes | P1 | **COMPLETE** | GOLF-142, GOLF-144 | **2026-09-19.** `26418aa`. Owner: "Start on the worker cache." Measured live, a single `hotelsViewport` query took 17.0s, another timed out past 45s, a third came back 502 — and almost every real request is for somewhere already looked at (panning back over a town, reopening a trip, a second visitor at the same course), so the same Overpass answer was being paid for repeatedly. Wraps all three Overpass modes (`heritage-pois`, `hotels`, `hotelsViewport`) in `withPoiCache()` using the **Cloudflare Cache API** (`caches.default`) — free, edge-local so a hit costs no round trip, and pure derived cache with no correctness requirement (a miss is just today's slow path). Also cuts call volume against Overpass, which DEC-016 makes a goal in its own right. TTL 24h. Cache API keys on a Request and a POST body isn't part of that key, so each mode builds a **synthetic GET URL** standing in for its parameters. `hotelsViewport` **snaps the bbox outward to a 0.01° grid** and passes the *snapped* box to the handler, so the cached cell genuinely covers every request that maps to that key and a hit is never missing edge pins — without snapping, an un-snapped bbox changes on every pixel of pan and the hit rate would be ~0. Deliberately **does not** cache ORS routing/geocoding (keyed by exact coordinates, so hit rates ≈ 0, and ORS's terms are a separate question) and **never caches a non-200** — an Overpass 521 must not be remembered for a day. CORS headers are excluded from the stored entry and recomputed per request, so a cached response can't leak one origin's `Access-Control-Allow-Origin` to another. Invalid input falls through to the handler so it still owns the 400. `scripts/test_worker_cache.mjs` (new): stubs `fetch` and the Cache API, copies the Worker to a `.mjs` in a temp dir to import it (no `package.json` in this repo, so no `"type":"module"`), 19 assertions — later extended to 28 by GOLF-147. |
| GOLF-145 | Hotel layer: click a pin to add that hotel to a trip day | P1 | **COMPLETE** | GOLF-142, GOLF-144 | **2026-09-19.** Once pins were visible (GOLF-144) they turned out to be inert — tooltip only, no click handler — so a hotel found by browsing the map couldn't be acted on; the only add path remained GOLF-96's picker, which starts from a day you've already chosen. `d0ba6ec`: pins now bind a popup with name, category, a `<select>` of trip days and an Add button. **The day picker is deliberate, not incidental** — this layer is browse-first, so unlike GOLF-96 the `dayId` isn't implied by where you clicked, and inferring it (e.g. first day without a stay) attaches to the wrong day whenever days are filled out of order. Owner chose this over auto-pick / prefill-Day-1 / only-clickable-from-within-a-day. Commits via `tripDayAddStop()` directly rather than prefilling `tbAddStop` the way `tbPickHotelCandidate()` does, since the popup has already collected the only extra fact that form would supply — nights default to 1, price blank, both editable on the itinerary row after; the pin going white→yellow via `render()`'s existing tint hook is the confirmation. Popup HTML is built on open, not per render (day list + in-trip state go stale on every trip change; 37 prebuilt popups would be wasted work). Verified against the live Worker: all days listed with place names, adding to Day 2 lands on Day 2 with correct lat/lng, persists to `golfmap:v1`, closes the popup and re-tints only that pin; already-in-trip and no-days-yet render explanatory text with no button; a hostile Overpass name is escaped and does not execute. |
| GOLF-144 | Hotel layer: surface why it isn't showing pins (zoom hint, loading, errors) + stop discarding slow responses | P1 | **COMPLETE** | GOLF-142 | **2026-09-19.** Raised as "hotels still don't work on prod". Live triage disproved that: three calls to the deployed Worker's `mode:'hotelsViewport'` returned 521, a >45s timeout, and 200 with 37 hotels after **17.0s**; handing those 37 POIs to the untouched `tbHotelLayerRender()` drew them correctly (screenshot). The renderer, pin SVG and tint logic from GOLF-142 were all fine — **the layer was failing silently around a slow/flaky upstream.** Two root causes, both fixed in `ed2c5a2`: (1) every no-pins outcome was invisible — below `HOTEL_LAYER_MIN_ZOOM` the fetch cleared and returned, upstream errors hit a no-op `catch`, and since the app opens at zoom 5 the toggle appeared completely dead from the default view. Added one status pill on the map (`.hotel-status`, `role=status`/`aria-live=polite`, appended to the Leaflet container): zoom hint, in-flight spinner, empty-result note, visible error. (2) the staleness rule dropped a response whose token no longer matched, so against a ~17s upstream *any* pan during the wait discarded the result and restarted — the race was effectively never won. Replaced with an ordering guard (`_hotelLayerDrawnSeq`) plus a viewport-overlap test, so a late response still draws while nothing newer has drawn and its area is still on screen. Also added a 25s `AbortController` so a hung query fails visibly; deliberately **no retry** (DEC-016 fair-use). Verified locally against the live Worker: zoom hint at z6, spinner in flight, 37 pins drawn *after panning mid-flight*, error pill on forced failure, yellow-tint-on-trip unregressed; `check_js`/`test_data` pass. **Does not fix the 17s upstream itself — that is GOLF-143.** |
| GOLF-143 | Migrate Worker's geo backend from OpenRouteService + Overpass to Google (Maps/Places) | TBD | **IDEA — owner is writing their own requirements; hold, no implementation yet. BLOCKED on a basemap decision — see 2026-09-19 note.** | GOLF-45 (ORS directions), GOLF-96/142 (Overpass hotels), heritage POIs | **2026-09-18, owner-initiated:** owner did their own usage-cap comparison (public Google pricing pages + a Gemini walkthrough, not yet a live Google Cloud project/billing/API key) and is currently leaning toward moving **everything** the Worker proxies — ORS driving directions, ORS geocoding (the known intermittent-403 endpoint), and all three Overpass-based features (heritage POIs, GOLF-96 hotel picker, GOLF-142 ambient hotel layer) — over to Google. Current real usage is tiny (owner + occasionally one other tester), which is the basis for the "unlikely to hit caps" call. Owner is writing up their own requirements before this goes further — **do not scope or implement until that lands**; when it does, it needs a DEC- record (this replaces the ORS-Worker architecture CLAUDE.md currently documents) and should probably be split into independently-shippable pieces (directions vs geocoding vs each Overpass feature) rather than one big-bang swap, since they have very different Google API cost/quota shapes (Places is billed separately from Directions/Geocoding and is usually the expensive one). **2026-09-19 — terms checked, this is the blocker:** owner added a Google API key to the Worker and asked whether to start with the basemap. Read both the [Maps Platform ToS](https://cloud.google.com/maps-platform/terms) and the [Service Specific Terms](https://cloud.google.com/maps-platform/terms/maps-service-terms). ToS §3.2.3(e) "No Use With Non-Google Maps" is restated **per API**, and every API this migration needs carries its own clause: Directions §4.2, Geocoding §6.2, Places (Legacy and New) §14.2, Routes §19.2 — each paired with a "Use without a Google Map" clause, i.e. no map is fine, **a non-Google map is not**. This app draws on Leaflet + Esri tiles (`js/map.js:56`), so **switching the basemap to Google is a hard prerequisite for using any of these, not an optional final step** — roughly 45 Leaflet call sites across 10 files (`divIcon` pins, `leaflet.markercluster`, polylines, circleMarkers, controls, popups), loss of the Esri layers, the API key moving into the browser behind referrer restrictions (reversing CLAUDE.md's "no API key ever reaches the browser"), and a new per-map-load billing line the owner's cost maths didn't include. Only carve-out is **Places UI Kit §15.1** (explicitly permitted with a non-Google map, overrides the general clause) but it's Google's own prebuilt UI components, not raw data for custom pins. Caching is also tighter than the app assumes: Directions §4.3 / Places §14.3 allow lat/lng for 30 days only; Geocoding §6.3.2 allows indefinite lat/lng + formatted_address but only per-end-user, isolated, and not as a substitute for a fresh call; and §3.2.3(a) bans pre-fetching/storing directions, geocodes and places data — which rules Google out for ever enriching `data/*.js` under the fetch-once pattern. **Recommendation on the table: don't migrate to Google; fix the three actual pains (ORS geocoding 403s, Overpass latency/fair-use, GOLF-118 ferry routing) with providers that carry no non-Google-map restriction (Mapbox / Photon / Pelias / Geoapify), each a Worker-internal swap with the front end untouched.** Awaiting owner's decision; needs a DEC- record either way. |
| GOLF-99 | Trip sharing v1 — shareable read-only trip (map overview, day-by-day costs, itemised total) | P1 | SCOPED | — | Explicitly a "get a demo out" v1, to be redone properly later. A `#share=…` read-only view already exists — confirm how much of this is already live before building. Scoped in archive Phase 31 (line 1). |
| GOLF-100 | Group / per-person costing | P1 | DONE | GOLF-99 | Per-person "£X pp" figure under every Costs-tab amount (line rows, category headers, fuel, grand total), shown when group size > 1, driven by the toolbar group-size control. GOLF-74 per-room model resolved from code: GOLF-91 replaced it with per-person-per-night × group size, divides cleanly everywhere. `js/trip-ui.js` `costPP()` / `costGroupHTML()`, `js/trip-share.js` for the shared view. Merged to `main` (2026-09-08). |
| GOLF-101 | Course imagery sourcing — written proposal | P2 | SCOPED | — | Copyright-safe options for course photos. Proposal only, not build. Archive Phase 31. |
| GOLF-103 | Hotel mapping — data source + visual treatment (options workshop, then build) | P2 | **Data source (v1) decided 2026-09-17 — Overpass, see DEC-016.** | — | Extends GOLF-96 (Overpass pins + manual price). Travelpayouts/Hotellook (DEC-014) turned out to be discontinued (DEC-015). **Re-decided 2026-09-17: Overpass** (DEC-016) — no signup, no cost, no caching restriction, Worker pattern already exists; coverage is the accepted known weakness, revisit (Google Places / other affiliate networks) if it proves inadequate in practice. Visual treatment unchanged: toggleable pin layer, no price shown. |
| GOLF-104 | Backend / accounts / server-side persistence — direction paper | P2 | DISCOVERY | GOLF-103 direction settled | Move off localStorage-only (reopens DEC-004). Prerequisite for US expansion, cross-device trips, booking attribution, email. Big refactor. **Sequenced: post go-live, AND after the hotel/POI direction (GOLF-103) is clear** so the persistence model is designed around known data needs. Written proposal only for now: options (managed BaaS vs minimal API + DB), auth model, migration of existing localStorage trips, cost, impact on the "zero-backend static" constraints. |
| GOLF-105 | Basemap upgrade — Esri tiles + default/satellite layer toggle (keep Leaflet, no API key) | P2 | COMPLETE | — | **Shipped & merged to `main` 2026-09-10** (`8490f33`, via branch `golf-105-esri-basemap`). Both Leaflet maps (`js/map.js`, `js/trip-share.js`) moved off the public OSM tile server (R-8) to **Esri's keyless `server.arcgisonline.com` raster tiles** — no account, no token — via a shared `esriBaseLayers()` factory + a `L.control.layers` toggle (top-right) on both maps. Two base layers: **Default** (Esri World Street Map, default) + **Satellite** (Imagery Hybrid = World_Imagery + World_Boundaries_and_Places + World_Transportation in an `L.layerGroup`). Per-layer attribution swaps on switch; `maxZoom`/`maxNativeZoom` 19; `detectRetina:true`; no CSP file exists so none updated. **Requirement change during build (owner, 2026-09-10):** toggle labels changed from "Street"/"Imagery Hybrid" → **"Default"/"Satellite"**; owner asked for a true OSM-*style* default — Esri's OSM-style basemap is **vector-only** (needs MapLibre) so that's declined here and folded into GOLF-106; the raster Default stays Esri World Street Map. Light Gray Canvas / World Topo still deferred (one-line adds). R-8 downgraded in `RISKS.md`. Owner ran the merge (implicit O1 accept); look-check on the live GitHub Pages deploy still worth a glance. Handover: `HANDOVER-GOLF-105.md`. |
| GOLF-106 | Vector basemap / MapLibre GL migration | P3 | IDEA | GOLF-105 | **Also now carries the "true OSM-style default basemap" want** — Esri publishes an OSM-style basemap but only as keyless **vector** tiles (`basemaps.arcgis.com/.../OpenStreetMap_v2/VectorTileServer`, PBF), which need MapLibre GL; declined for GOLF-105 (raster-only) 2026-09-10. The smooth continuous-zoom / rotate / crisp-label look = vector tiles, which need the **MapLibre GL** engine (Leaflet can't render them). Full port re-implements markers, clustering, the bespoke rail-line geometry, popups, bounds-fit, mobile list/map toggle, and the share-view map — all 9 `L.`-using JS files — multi-day refactor + regression risk. **Legit long-term goal, not overkill**, but post go-live; revisit when the map carries heavier load (US data, toggleable layers) — likely around/after GOLF-104. `maplibre-gl-leaflet` shim is a lower-risk middle path with limited payoff. **BA/PM steer (2026-09-09): do GOLF-105 first, do not skip straight to 106.** 105 closes R-8 in ~2 hrs; 106 is a week+ multi-file engine port with real regression risk and stays post-go-live. 105 is not wasted work — the MapTiler/Stadia account + key set up for 105 is the same one 106's vector tiles use, and 105's code delta is a one-line `L.tileLayer` URL swap ×2. Skipping to 106 leaves R-8 open through go-live. |
| GOLF-107 | Pre-go-live UX refinement pass (umbrella) | P1 | SCOPED | — | Owner's bug list 2026-09-07 triaged into GOLF-108…117 + a GOLF-100 addition. All code/UX, no hosting. Full scope + acceptance criteria: `GOLF-107-uxpass.md`. |
| GOLF-108 | Course pins suppressed once a trip exists (map visibility, Discover + Itinerary) | P1 | DONE | — | Branch `golf-108-109-course-pin-visibility` (2026-09-09), pending owner review on Cloudflare preview before merge. "Nearby courses" toggle (default ON) + `bgCoursePins` pane. Scope + impl summary: `GOLF-107-uxpass.md`. Closes RISKS R-9. |
| GOLF-109 | Multi-course venues — all courses show on map (Turnberry Ailsa + KRTB) | P1 | DONE | GOLF-108 | Symptom of GOLF-108; `passes` needed no change. `tripShow()` also moved to the shared jittered-coord lookup. Same branch as GOLF-108. |
| GOLF-110 | Hide nearest-railway-station feature behind a flag (dormant, not deleted) | P2 | DONE | — | DEC-008. `RAIL_FEATURE=false` in `js/map.js`: rail polylines/station dots/labels never built, `restyleRail()`/`zoomend` short-circuit, `drawLink()` no-ops, "By rail" popup row gated, `js/explore.js` toggle wiring gated. Data + functions kept; flip flag → full restore. Merged to `main`. |
| GOLF-111 | Course marker redesign — golf ball on a tee | P2 | DONE | — | `golfPinSVG()` (`js/util.js`) rebuilt from the owner's reference PNG: flat inline SVG, white ball + dimple hints, coloured tee, dark outline, subtle drop-shadow, no gradients. `pinFor()` (`js/map.js`) anchors at the tee tip and passes `{ranked, tint}`; ranked keeps the 30-vs-22 size step + a gold halo; `pinStateTint()` maps in-trip → blue / played → green / want → amber tee (default red), refreshed live by `render()`'s existing `setIcon` loop. `.mcluster` badge restyled white/dark to match. Merged to `main` (2026-09-08). |
| GOLF-112 | City search — click result to focus map (not only add stop) | P2 | DONE | — | Place-name link (`.tb-unified-place-focus`) flies the map + drops a temp 📍 marker (cleared on next search / manual pan-zoom / Escape), no trip stop; "Add to trip" stays a separate button. Click also re-scopes Discover "Nearby" to the place (`tbPlaceAnchor`). geocode-403 degradation untouched. `js/map.js` `tbFocusPlaceOnMap()`, `js/trip-add.js`, `js/trip-ui.js`. Merged to `main` (2026-09-08). |
| GOLF-113 | "Discover by Region" — region list must match selected nation | P1 | DONE | — | `tbRegionOptionsHTML()`/`tbRegionsForNation()` in `js/trip-ui.js` filter regions to those with ≥1 course in `state.nation`; no nation → `<optgroup>` by nation. Switching nation drops a now-invalid `tbRegion`. Merged to `main`. |
| GOLF-114 | Header + nation-pill layout — spacing, full-width, even/dynamic distribution | P1 | DONE | — | `.nation-pills` now `display:grid; grid-template-columns:repeat(var(--nation-count),1fr)` — `--nation-count` set inline from `NATIONS.length` by `tbNationPillsHTML()`, so a 4th/5th nation redistributes with no CSS change. `--sp-6` top padding = header→pills breathing room, `--sp-5` sides match the gutter, pills `min-height:var(--tap)` (44px), long labels wrap and stay equal height. Verified at 360px. Merged to `main`. |
| GOLF-115 | Long trip name gets clipped | P1 | DONE | — | Trip-menu summary wraps the name in a `.tb-drop-label` span that ellipsises inside the fixed toolbar cell (row never grows) with the full name as a `title` tooltip; share payload now carries `nm` and the shared view shows the full name. Merged to `main`. |
| GOLF-116 | Costs tab — consolidate hotel stays into one line (`Hotel A (R300/night × 2) R600`) | P1 | DONE | — | `tripCostLineItems()` (`js/trip-ui.js`) groups hotel night-items by `stayId` (fallback `name + lat/lng` 3dp) into one row `£X/night × N nights`. Grand total unchanged (sum of the same per-night values). Merged to `main` (2026-09-08). |
| GOLF-117 | Research spike — ferry legs (Scottish islands, Islay↔Ireland) | P3 | COMPLETE | — | Done 2026-09-08 — `GOLF-117-ferry-spike.md`. Findings: ORS `driving-car` already crosses `route=ferry` ways (verified live: Islay/Mull/Cairnryan yes, Arran no) but inconsistently and with no timetable awareness; ferry legs are detectable by adding `extra_info:["waytypes"]` (code 9) to the Worker — small change; no free CalMac API (TNDS XML only); Kintyre Express (Islay↔Ireland) is passenger-only + seasonal; Port Ellen shut 2026–29. Recommendation: flag-only v1 → GOLF-118. |
| GOLF-118 | Flag ferry legs + split ferry/drive time in the itinerary | P3 | READY | — | Approved by owner 2026-09-08. Worker adds `extra_info:["waytypes"]`, returns `hasFerry` + `ferryMinutes` + `ferryMiles` (backwards compatible). Itinerary drive-leg rows with a ferry show a **"⛴ This route has a ferry"** tag and split the leg time into *"Xh Ym driving + ~Zm ferry"*. Operator link from a curated `data/ferries.js` is nice-to-have, not required for v1. NO timetables/fares (parked until date selection). Known: ORS sometimes skips a ferry and routes long — `hasFerry:false`, acceptable. Scope: `GOLF-117-ferry-spike.md` §4. Handover: `HANDOVER-GOLF-118.md`. Post-go-live. |
| GOLF-119 | DotGolf club-finder API — geographic coverage audit | P3 | ON HOLD | GOLF-121a | Deprioritised 2026-09-09 alongside GOLF-121b/c (GB & Ireland coverage deemed sufficient). Keep for whenever a new-nation expansion is actually greenlit. XS desk-research ticket (owner ask 2026-09-08). Deliverable: `docs/project/GOLF-119-coverage-audit.md` — which national bodies expose the DotGolf API (base URL, whether a bulk list works vs name-only), a definite yes/no on **NZ (`golf.co.nz`) + Australia (`golf.org.au` / GolfLink)**, and a one-line verdict per other candidate geography (US = GHIN/USGA; continental Europe per-country?). Feeds GOLF-121c. No shipped-code changes; throwaway probe script only. **Sequence: after GOLF-121a** (SA full pass teaches the bulk-pull pattern). Handover: `HANDOVER-GOLF-119.md`. |
| GOLF-120 | Green-fee schema v2 — granular seasonal / twilight / time-band fee data | P1 | **Phase 2 SHIPPED to `main` 2026-09-10** (helpers + display + batch-1 merge). Batches 2+ continue as GOLF-98. | — | Owner 2026-09-08: spot-checks show current `fee:{wd,we}` values inaccurate. Want a richer source structure capturing high/shoulder/winter season, weekday/weekend, twilight, time bands, 9-hole, guest-of-member, cart — displayed figure derived as "highest applicable". Forward-looking toward per-tee-time planning/booking. **Big job:** schema design (spec + owner sign-off) FIRST, then re-research every course incl. the England Top 100 already migrated. **Gates the remaining-nations green-fee work — pause GOLF-98 continuation until v2 schema is locked.** Research via the established fetch-once → JSON → manual-merge pattern (NOT scraping — BRS Golf ToS, see GOLF-98). **2026-09-10 (owner ran it overnight, "as flexible a schema as possible" + start top 30% per region):** `docs/project/GOLF-120-schema-v2-proposal.md` written — `feeV2` shape (one flat `rate` record; optional season/day/time-band/holes axes; optional `cart`/`playerType`; per-object provenance; `isFrom` for "from £X"), full "highest applicable" derivation rule (§3, no-date / date / date+time), migration plan, 7 open questions for owner, recommendation. Batch-1 research (126 courses = top ~30% by ranking of England Top 100 / Scotland / Ireland / Wales / SA) via 9 background Haiku agents → `docs/project/GOLF-120-fees-batch1.json` (86 published-rates, 25 estimated, 15 poa). **NOT merged** — no shipped file touched; §3 is the Phase-2 merge plan. **Owner answered all §5 questions 2026-09-10:** max granularity (exact season months + time bands); no bank-holiday list (weekend = Sat/Sun, calendar-API hook later); in-season peak shown as headline but labelled "Up to £X"; mandatory buggy = its own Costs line item, never folded into the fee; society/group-only courses = `poa`; England Top 100 ranks 35–114 deferred to a "batch 2" (reminder routine `trig_01KjZDrrJ3wYrk8EQ3nf26Z9` fires Tue 2026-09-16 22:00 London, just before the weekly reset). **Next:** a dedicated Phase-2 handover for the `js/trip-geo.js` helpers + `SCHEMA.md` doc + per-nation merge. **2026-09-10 Phase 2 shipped to `main`:** `feeV2` reader + §3 derivation in `js/trip-geo.js` (tier 1, ahead of v1 `fee` and `wd`/`we`); `scripts/test_fee_v2.js` (24 assertions); display wired through popup / tooltip / `feeNum` (Explore slider/filter/sort) / Costs tab (`feeRangeLabel` learns `from £X` / `Up to £X`) / itinerary; mandatory-buggy → own "Compulsory buggy" Costs line + tag (owner Q6; no batch-1 course is actually `mandatory`, so latent until data says so); `GOLF-120-fees-batch1.json` merged into all 5 `data/*.js` (34 top100 / 30 sco / 25 ire / 12 wal / 25 SA), additive beside `wd`/`we`/`fee`; `test_data.js` validates `feeV2`. Merge-time normalisations: `playerType` `adult`/`visitor-sa` → `visitor` (St Enodoc, Erinvale); Southerness `"HH:MM-HH:MM"` timeBands → enum + `bandStart`. `SCHEMA.md` updated (`feeV2` row added, v1 `fee` marked legacy/frozen). **2026-09-10 popup fee-display redesign SHIPPED to `main`** (owner disliked the original two-box + all-caps pill): single "Green fee" box when weekday == weekend headline else two "Weekday"/"Weekend" boxes; amount is the prominent figure (20px/700); small sub-label "Confirmed" (researched) / italic "Estimate" (estimated) / none for POA; `.fee-pill` removed (`js/map.js` `feeBoxSpan`→`feeBlock`). Number-accuracy owned by `docs/project/GOLF-120-fee-testing-plan.md` (verification passes tracked under GOLF-98). |
| GOLF-121 | Course completeness — bulk DotGolf course pull, per country (SA first) + missing sister / 36-hole-club courses | P2 | 121a + 121d COMPLETE; 121b/121c POSTPONED; 121e PARKED (greenlit) | GOLF-119 for 121c | Owner 2026-09-08/09. Split into sub-tasks. **121a — South Africa full pass — COMPLETE (merged to `main` `6bc2fd8`, 2026-09-09):** `data/courses-southafrica.js` 99 → 421 (near-complete national coverage from Handicap Network Africa); `--all` resumable mode added to `fetch_south_africa_golf_clubs.py`; coordinates corrected against OpenStreetMap (`fetch_sa_golf_overpass.py`); `spec:"Unknown"` (hole counts never supplied); 3 new map regions (Free State, Limpopo, Northern Cape); Randpark split into its two 18s; `wd`/`we` placeholders + `conf:"est"`, no `fee:{…}`, no fabricated `t100.za`. Notes + open items: `GOLF-121a-notes.md`. **121d — SA map ringfenced to the top 100 — DONE (branch `golf-108-109-course-pin-visibility`, 2026-09-09):** owner ask ("too many SA courses now — show only the top 100"). The ~107 courses on the union of satop100courses.com's "Top 100 by ranking" + Top100GolfCourses.com's SA list get `zaRanked:1`; `courseShownOnMap()` (js/explore.js, js/trip-route.js) hides every other `topSouthAfrica:1` entry from the map — they stay in the data file for a future "show all" toggle. `t100.za` re-derived from the curated 1–19 to a dense 1–107 combined ranking (provenance table in `GOLF-121a-notes.md`). Two ranked courses absent from the dataset (no coords): Beachwood, Boschenmeer. **121b — UK & Ireland curation strategy (write-up) — POSTPONED (owner 2026-09-09):** current coverage is judged sufficient — England ~120 flagged (ranked Top 100 fully covered + 13 just-outside), Scotland 100, Ireland 83, Wales 38. No "too many / too few" problem for GB & Ireland, so the strategy write-up and any per-nation bulk pull are shelved. Revisit only if the inclusion bar changes or gaps are reported. **121c+ — per-nation bulk pulls — POSTPONED, same rationale** (was: Ireland next after SA, England last). Bulk pull returns no green fees. Multi-course clubs: record gaps, don't fabricate siblings. NOT the same as GOLF-109. Handover: `HANDOVER-GOLF-121.md`. **121e — Australia — PARKED, greenlit in principle (owner 2026-09-09, "I like the look of the Australia results").** Research pull done & reviewed: `GOLF-121-australia-research.md` (the Top 100 table) + `GOLF-121-australia-sources.json` (raw). Pickup note with open decisions + implementation outline: `HANDOVER-GOLF-121e-australia.md`. Not started — adds a 4th nation pill, `data/courses-australia.js` (~100 ranked courses, `auRanked:1` + `t100.au`), AU regions, and AUD currency plumbing. Effectively single-source (Golf Australia Magazine 2026) — decide on a second ranking before merge. |
| GOLF-122 | Stale content after deploy — service-worker update lag | P1 | COMPLETE | — | Merged to `main` (`0ea5c11`, 2026-09-08), owner-verified against a live deploy 2026-09-09 (single-reload freshness + offline both confirmed). `sw.js` v6 — navigation requests (`req.mode === 'navigate'`) are network-first (try network → `stripRedirect` cleanup → refresh cache → return; offline falls back to `caches.match(req)` then the canonical `./london-golf-map-v5_1` shell). Everything else stays cache-first. Redirect-cleanup extracted to a shared `stripRedirect()` helper reused by both `install` and `fetch`. Handover: `HANDOVER-GOLF-122.md`. |
| GOLF-128 | Discover → select course → nearby-courses workflow — feel / flow refinement | P3 | IDEA — needs owner design input | — | Owner (2026-09-10): "some minor behaviour that I dont like with the workflow between discover -> selecting a course -> nearby courses but I need to think about how I want it to look." Placeholder only — not actionable until the owner specifies what should change (marker/camera behaviour on course select, how "nearby" re-scopes, what returns to the discover list). Do not start without a spec. |
| GOLF-133 | General UI review pass — post-redesign sanity check | P3 | COMPLETE — split into GOLF-134–140 | GOLF-130, GOLF-127 | **2026-09-13: consultant-style UI pass done, findings written up (`GOLF-133-ui-review.md`) and split into 7 individual tickets — GOLF-134 through GOLF-140 (see rows below).** This ticket's job (go look, propose fixes) is done; track follow-up work on the individual tickets, not here. |
| GOLF-134 | Course photos and club logos render as broken images sitewide | P0 | COMPLETE | GOLF-133 | **2026-09-13 fixed:** `escUrl()` (`js/util.js`) regex extended to also allow bare same-origin relative paths (no leading slash, no `:` — so a disguised `javascript:` payload still can't pass). Verified via `popupHTML()` injection: Yelverton's photo and club logo now render correctly. `node scripts/test_data.js` + `check_js.js` pass. Original finding, kept verbatim below for context. **From GOLF-133's UI review, verbatim:** Open any course popup with a photo or logo (e.g. Yelverton, `data/courses-top100.js`) and both images render as the browser's broken-image icon. Verified via browser JS: the rendered `<img src>` is literally `#`, not the real path. **Root cause (confirmed in code):** `escUrl()` (`js/util.js:80-83`) allowlists only `https?:`, `mailto:`, `/`, and `#` — anything else is replaced with `#`. But every `photo.src` and `logo` value in every `data/*.js` file is a bare relative path with no leading slash (`images/courses/yelverton.jpg`, `images/clubs/*.jpg` — confirmed across all five nation files, ~260 photos / 71 logos). None of them ever match the allowlist, so `escUrl()` silently kills every one, in production, not just the local preview it was found on. **Why it matters:** this is the single most visible thing in the app — course photos are the hero content of every popup — and it's been dark site-wide since whenever `photo`/`logo` fields were added. A tester's first course click shows a broken-image glyph instead of the course. **Proposed fix:** one-line change to `escUrl()`'s regex to also allow bare relative paths (e.g. add an alternative matching `^[\w.\-]` for same-origin relative URLs), or prefix every `photo.src`/`logo` value with `/` at the data layer and extend the allowlist's `/` case to also match without a leading slash. Worth `grep`-confirming no other field (course `site`, `sourceUrl`) has the same relative-path pattern before treating this as fully scoped. |
| GOLF-135 | Single-course trip zooms the map to near-blank grey tiles | P1 | COMPLETE | GOLF-133 | **2026-09-13 fixed:** added `maxZoom:14` to every `fitBounds()` call site in `js/trip-route.js` (3 sites) and `js/trip-geo.js` (1 site), matching the guard already in `trip-share.js`. Verified via `map.getZoom()` returning 14 (was 19) after adding a single course to an empty trip. `js/trip-ui.js`'s nation-pill `fitBounds()` deliberately left unchanged (fits many points, out of scope). Original finding, kept verbatim below for context. **From GOLF-133's UI review, verbatim:** Wishlist/add a single course to a fresh trip, then switch to the Itinerary or Costs tab. The map jumps to zoom **19** (confirmed via `map.getZoom()`) centred tight on that one course — at that zoom the Esri tile layer has little to no coverage outside towns, so the map renders as mostly blank grey with the pin pushed toward an edge. Looks broken, not just "very zoomed in." **Root cause:** every `fitBounds()` call site (`js/trip-route.js`, `js/trip-ui.js`, `js/trip-geo.js`) passes only a `padding` option, no `maxZoom`. Leaflet's `fitBounds()` on a single-point (zero-area) box zooms to the layer's max by default. Notably, `js/trip-share.js:236` already guards against exactly this (`if(pts.length>1) m.fitBounds(...)`) — the shared read-only view was evidently patched for this, but the main app's several `fitBounds()` call sites weren't. **Why it matters:** this is the very first thing a new trip looks like — add one course, and the map that's supposed to sell the trip goes blank. **Proposed fix:** add `maxZoom` (something in the 13–14 range, town-level) to the `fitBounds()` options at each call site, matching the existing single-point special case already proven out in `trip-share.js`. |
| GOLF-136 | Mobile Costs tab: floating "Show map" button overlaps body copy | P2 | COMPLETE | GOLF-133 | **2026-09-13 fixed:** `.tb-pane{padding-bottom:64px}` added to the `@media (max-width:900px)` block in `london-golf-map-v5_1.html`, clearing the fixed `.mob-toggle` pill. Verified via mobile (375×812) screenshot at the bottom of the Costs tab — currency disclaimer fully readable. Original finding, kept verbatim below for context. **From GOLF-133's UI review, verbatim:** At 375px (Costs tab, scrolled to the bottom), the fixed/sticky "Show map" pill sits directly on top of the last two lines of the currency disclaimer ("...planned for a future update — for now amounts display in each course's own local currency"). The text is unreadable at any scroll position — the button doesn't move, so there's no way to read it on a phone. **Why it matters:** small, but it's exactly the kind of thing a beta tester on their phone screenshots and reports as "your app is broken," and it's on the Costs tab — one of the three core tabs. **Proposed fix:** add bottom padding/margin to the scrollable content equal to the pill button's height (+ safe margin) whenever it's visible, or move the pill to not overlap scrollable text (e.g. anchor above the tab bar instead of floating over content). |
| GOLF-137 | "Border" field (by-region search) has no visible unit | P2 | COMPLETE | GOLF-133 | **2026-09-13 fixed:** by-region label in `js/trip-ui.js` changed from `Border` to `Border (mi)`, unit now visible without hovering. Verified via mobile (375×812) screenshot. Original finding, kept verbatim below for context. **From GOLF-133's UI review, verbatim:** By-region search shows a plain `Border` label next to a number input (default `8`). The only explanation of what it does ("include courses just outside the region, within this many miles of its edge") is a hover-only `title` tooltip — invisible on touch devices, which is most of this app's likely usage. **Why it matters:** a tester on mobile has no way to discover what this field means or what unit it's in; worst case they assume it's broken or ignore it. **Proposed fix:** either put the unit inline in the visible label ("Border (mi)") or add a short static caption under the field, in addition to (not instead of) the existing tooltip for desktop. |
| GOLF-138 | "+ Wishlist" immediately schedules a costed Day-1 itinerary stop | P3 | CANCELLED — working as designed | GOLF-133 | **Owner decision 2026-09-13:** current behaviour is the intended model — the first course added via "+ Wishlist" to an empty trip auto-materializes as a scheduled Day-1 itinerary stop (green fee counted in Costs) rather than sitting unscheduled. Resolves the open question raised in GOLF-133's UI review; no code change. See DEC-012. |
| GOLF-139 | `.mcluster` cluster badge — still an open call from GOLF-130 | P3 | CANCELLED — keeping as-is | GOLF-133, GOLF-130 | **Owner decision 2026-09-13:** keep the plain numbered-circle cluster badge as-is; not restyling it to match the GOLF-130 teardrop pin. Resolves the open call carried over from GOLF-130's O1; no code change. See DEC-013. |
| GOLF-140 | Overall visual language is competent but generic | P3 | IDEA — needs owner steer | GOLF-133 | **From GOLF-133's UI review, verbatim:** the chrome (white cards, navy/blue pill buttons, gray body text, Manrope) is clean and consistent, and nothing here is broken — but nothing in the UI's own styling (empty states, header, panel chrome) gives it a distinct personality tied to golf or travel. Empty states ("Nothing on your wishlist yet," "No courses in that region yet — pick one above") are plain single-line gray text with no visual weight. **Why it matters:** lowest priority on the list by design — this is a "nice to have before real strangers see it" item, not a defect. **Proposed fix (if picked up):** not urgent enough to spec in detail; if the owner wants to invest, the highest-leverage single change would be the empty states (wishlist, region-not-picked, no-results) since those are what a first-time visitor sees before any data is on screen. A reference visual-direction mockup is being prepared separately to give the owner something concrete to react to before this is scoped further. |
| GOLF-35 | Custom domain / production hosting consolidation + hidden dev instance | P2 | Phase A COMPLETE | Phase B blocked on user buying domain | **Phase A shipped 2026-09-13 (GOLF-129).** `functions/_middleware.js` (Basic Auth, `dev`/`env.DEV_PASSWORD`, Preview-only via env-var scoping), `_headers` (`X-Robots-Tag: noindex,nofollow`) + `robots.txt` (`Disallow: /`), PWA/asset URLs already origin-agnostic (no change needed), no `CNAME` file existed (nothing to remove). **Owner decided 2026-09-13: `DEV_PASSWORD` not needed for this beta** — testers are a small known group, link-only + noindex is judged sufficient; they get the static production URL (`golf-map.pages.dev`) directly. Gate code stays in place, dormant, for if a future beta wants it. Phase B (custom domain, GitHub Pages retirement) still blocked on the owner buying a domain. Handover: `HANDOVER-GOLF-35.md`. |
| GOLF-102 | ORS Worker abuse hardening — CORS allowlist + rate limiting | P2 | Part 1 COMPLETE | GOLF-35 Phase B for rate limiting | **Part 1 (CORS allowlist) shipped 2026-09-13 (GOLF-129)** — `corsHeaders()` in `scripts/cloudflare-worker/ors-proxy.js` now reflects `Origin` only for `golf-map.pages.dev` / `*.golf-map.pages.dev` / localhost, else sends `Access-Control-Allow-Origin: null`. `request` threaded through every handler + the `json()` helper. Part 2 (rate limit rule) needs the Worker on the custom domain — still blocked on GOLF-35 Phase B. In `HANDOVER-GOLF-35.md`. |
| GOLF-129 | Beta launch readiness — password gate + noindex + tester limitations notice | P1 | COMPLETE | GOLF-35 Phase A, GOLF-102 Part 1 (bundled, not blocking — same handover) | **Shipped 2026-09-13.** GOLF-35 Phase A + GOLF-102 Part 1 done per `HANDOVER-GOLF-35.md` (see those rows). New scope: `tbBetaBadgeHTML()` (`js/trip-ui.js`) — a "Beta" pill in the Plan/Build navbar (`.tb-beta` `<details>`, reuses the `.tb-drop`/outside-click-dismiss pattern) opening a 4-point limitations panel (nation coverage, green-fee confidence, hotel-data coverage, localStorage-only), owner-copy verbatim. Verified absent from `#share=…` (that view never calls `renderTripBuilder()`) and non-overlapping at 360px. No feedback link/form (owner will contact testers directly). `node scripts/test_data.js` + `node scripts/check_js.js` pass. **O1 (set `DEV_PASSWORD`) decided not needed 2026-09-13** — testers are a small known group, link-only + noindex is sufficient; static production URL (`golf-map.pages.dev`) goes to testers directly, no password. **Owner tasks remaining: O2** (sanity-check panel wording on the production URL), **O3** (decide who gets the beta URL + when). Handover: `HANDOVER-GOLF-129.md`. |
| GOLF-130 | Course marker visual redesign — teardrop pin + flag, two-state colour | P2 | MERGED to `main` — owner sanity-check (O1) still open | Relates to GOLF-111 (2026-09-08 ball-on-tee redesign), partially resolves GOLF-127 (wishlist/anchor tint — ring still outstanding) | **Implemented 2026-09-13 on branch `golf-130-marker-redesign` (`7d94350`), reviewed and merged to `main` same day (`8137da5`, `sw.js` bump `98661dc`).** `golfPinSVG()` (`js/util.js`) rebuilt: teardrop pin + flag, true circle ~78% of teardrop width with a thin green border, flag scales with the circle via SVG transform; ranked gold halo + 30/22px size step carried forward. `pinStateTint()` (`js/map.js`) collapsed to the locked 2-state boolean (`TRIP.has(i)\|\|PLAYED.has(i)\|\|WANT.has(i)` → yellow, else white). Day-order number marker removed entirely: `tripShowOrdered()` (`js/trip-route.js`) now draws the shared `pinFor(i)` pin instead of the numbered `tripGolfMarkerIcon()` badge (deleted, plus its `.trip-golf-marker`/`.tgm-*` CSS) — popup/tooltip/click-highlight behaviour preserved; sequence now only visible via the Itinerary tab list + route-line order. `trip-share.js`/`explore.js` have no separate marker code so the new shape/colours propagate everywhere, including `#share=`, with no extra work. Dev flagged one open call for the owner: the `.mcluster` cluster count badge was left as a plain numbered circle (judged it doesn't need to echo the new pin shape) — confirm or override. `test_data.js` + `check_js.js` pass; visually verified in-browser. **GOLF-127 (the anchor-ring half) is now also closed**, 2026-09-13 — see "Recently completed". Handover: `HANDOVER-GOLF-130.md`. **Next: owner confirms the live look on production (O1: `.mcluster` badge call + overall sanity check), then this can close as COMPLETE.** |
| GOLF-131 | Itinerary "nearby courses" — default off, live-update as the map pans | P2 | COMPLETE | GOLF-108 (introduced the toggle) | **Retroactively logged 2026-09-13** — implemented and merged straight to `main` (`cc0bd85`) without going through BA/PM discovery; documenting after the fact from the commit so the backlog matches reality, not because this is the process to repeat. Report: moved north from a southern-Ireland trip wanting to add Royal Portrush; it didn't surface on the Itinerary map until searched for, because "nearby courses" only anchored on the trip's own stops. Fix: `TB_SHOW_NEARBY_DEFAULT` (`js/trip-ui.js`) flipped `true`→`false` (opt-in, was noisy on by default per GOLF-108); `tbItinNearbyAnchorPts()` (`js/trip-route.js`) now also anchors on the current map centre; `tbDrawMap()` takes an optional `fit=false` and a debounced `moveend`/`zoomend` listener calls it (no `fitBounds`) while Itinerary + the toggle are both on, so the nearby set live-refreshes without fighting the user's pan. No backlog discovery, acceptance criteria, or handover doc preceded this — see GOLF-128, the open "discover → nearby" workflow placeholder, for the standing owner discomfort with this general area; worth folding this pattern into that discussion if further changes are wanted here. |
| GOLF-142 | "Show hotels" toggle layer on the map (Overpass) | P2 | **COMPLETE** | GOLF-96 (existing Worker POI pattern) | **Client-side merged to `main` 2026-09-17** (`0604b48`, branch `golf-142-hotel-layer` — see that commit for the full build summary). All acceptance criteria verified in-browser pre-merge (toggle wiring, zoom gate, debounce, toggle-off clears pins, GOLF-96 picker unaffected). **Blocker found post-merge:** the live Worker (`geofftheworker.stefand94.workers.dev`) kept returning the old fallback error (`"origin and destination must both be..."`) for `mode:'hotelsViewport'` requests — meaning it wasn't running the merged code, even though `mode:'hotels'` (GOLF-96, unrelated) works fine, proving the Worker itself is reachable and healthy. A background agent independently confirmed the `hotelsViewport` handler logic is correct by running it directly against live Overpass (36 real hotels for a St Andrews bbox) — this is a **deploy problem, not a code bug**. Troubleshooting so far: (1) owner checked the Cloudflare dashboard, found build `ce5cf37f` marked succeeded — but `curl` still showed old behaviour after it, so that build likely didn't correspond to the actual merge commit; (2) pushed an empty commit (`99a24b5`) to force a rebuild — did **not** trigger one, because Cloudflare's Git integration only builds on pushes that touch `scripts/cloudflare-worker/` (per its own README); (3) pushed a trivial comment-only change to `ors-proxy.js` (`41ed2f6`) to force a real rebuild — owner confirmed in the dashboard that **this one did deploy** — but `curl` against `mode:'hotelsViewport'` still returned the old error immediately after. **Unexplained as of pause:** a dashboard-confirmed successful deploy of the current `ors-proxy.js` not taking effect on the live URL. Possible causes to check next session: multiple Workers/routes bound to the same custom domain (an old one still serving), a stale edge/CDN cache on a POST endpoint, the Git integration deploying to a *preview* environment rather than the production route, or a second, disconnected copy of this Worker's code pasted directly in the dashboard editor overriding Git-integration deploys. **Next step: re-test `curl -X POST https://geofftheworker.stefand94.workers.dev -d '{"mode":"hotelsViewport","bbox":[56.335,-2.82,56.345,-2.79]}'` first — if still failing, check the Worker's dashboard for multiple environments/routes/versions before pushing any more code.** **2026-09-18 session:** re-ran that curl — still the old fallback error, so the Worker-deploy blocker is confirmed still live; this session had no Cloudflare dashboard/`wrangler` credentials to take the investigation further. While blocked on that, did the pin-styling half of the layer that didn't depend on it: hotel pins (`js/hotel-layer.js`) now use the same teardrop-pin-with-circle shape as `golfPinSVG()` instead of a plain `circleMarker`, via a new shared `pinShapeSVG()` helper in `js/util.js` (golf pin output byte-for-byte unchanged — verified in-browser) — blue body/outline, 🏨 glyph in the circle (new `hotelPinSVG()`), white circle by default, yellow (`tbHotelInTrip()`, matches a viewport POI to a trip hotel item by lat/lng) once that hotel is added to the trip. A new `tbHotelLayerRefreshTint()`, hooked into `render()`'s single hook point (`js/explore.js`), flips a pin's circle colour immediately on trip add/remove without a re-fetch. Verified by mocking `tbHotelLayerRender()` with fake POIs in-browser (the live Worker still can't be exercised for real data) — pin renders correctly and re-tints on trip add. **Resolved 2026-09-18** — owner redeployed the Worker from the Cloudflare dashboard and it took effect: `curl` against `mode:'hotelsViewport'` now returns real Overpass hotel POIs (34+ for a St Andrews bbox), and `mode:'hotels'` (GOLF-96) still works unaffected. Root cause landed on "a redeploy fixed it" rather than a specific misconfiguration ever being pinned down in the dashboard — likely a stuck version/traffic-routing state that a fresh manual redeploy cleared. Verified end-to-end live on `golf-map.pages.dev`: toggling "Show hotels" and panning to St Andrews shows 36 real hotel pins in the new blue/🏨 style, confirming both the Worker and the client-side pin-styling work (this session's earlier note) together. |
| — | Green-fee `feeV2` data beyond batch 1 (London) | P2 | QUEUED — see GOLF-98 in `IN_PROGRESS.md` | GOLF-120 Phase 2 (done) | Continuation of GOLF-98 per-nation, now targeting `feeV2`. **England Top 100 now fully done** (all 114 courses have `feeV2` — batch 1 + batch 2, 2026-09-13); SA's `zaRanked` courses also done (2026-09-13); **Scotland/Ireland/Wales beyond top ~30% now also done** (all 220 courses have `feeV2` — batch 3, 2026-09-14). Remaining: London only. |

## Recently completed (one line each — prune quarterly)

- Map legend removed entirely (owner request, 2026-09-17) — deleted `explore.js`'s `legend` `L.control` and its CSS; this also removed the TfL/National Rail open-data attribution and course-data sourcing disclosure text it carried, not just the flag/station/rail-line key. Merged directly to `main` (`1b4e398`), no ticket — simple, self-contained UI removal.
- GOLF-98/GOLF-120 batch 2 — England Top 100's remaining 80 courses (of 114) now have `feeV2`, closing out England entirely (34 from batch-1 + 80 here). 7 background Haiku agents researched in parallel; merged additively into `data/courses-top100.js` by course name. Aggregator-only sources (golfshake.com etc.) downgraded from the agents' self-reported `published-rates` to `estimated`/`published-from-only` before merge, per the schema's own confidence definitions. 4 minor schema-conformance fixes (Monday-specific rates folded into `weekday`, an 11-hole rate dropped — schema has no slot for either). `test_data.js` (879 courses) + `check_js.js` pass. (2026-09-13)
- GOLF-98/GOLF-120 batch 3 — all 220 Scotland/Wales/Ireland courses now have `feeV2` (153 newly researched: 69 Scotland, 26 Wales, 58 Ireland). 13 background Haiku agents researched in parallel (6 Scotland, 2 Wales, 5 Ireland); one relaunch round needed mid-pass after a session-wide API rate limit killed most of the first round (no data lost, re-run cleanly once quota reset). Merged additively into `data/courses-{scotland,wales,ireland}.js` by course name (with HTML-entity unescaping for `&`-containing names like "Fortrose & Rosemarkie"). Zero schema-conformance fixes needed this time — the day/holes enum rules and aggregator-confidence self-downgrade rule were embedded directly in every agent's prompt up front, based on the England batch-2 lesson. Two manual merge fixes for courses that were the last array entry in their file (Prestatyn, Westport Golf Club — the line-splice script's edge case). `test_data.js` (879 courses) + `check_js.js` pass. (2026-09-14)
- GOLF-127 — redundant gold anchor-ring removed: `tripShow()` (`js/trip-geo.js`) now only draws the ring when `!pinStateTint(anchor)`, since a wishlisted/in-trip anchor's pin is already yellow (GOLF-130); the browsing-before-wishlisting case keeps the ring as its only "this is your anchor" cue. Owner tested and merged to `main` (`golf-127-132-sa-fees`, `5f0878a`, 2026-09-13). (BUGS.md)
- GOLF-132 — every deploy now clears every visitor's saved trip (DEC-011): `scripts/update_sw_cache_version.py` stamps `sw.js`'s content hash into a new `js/app-version.js` (`APP_VERSION`, loaded before `js/state.js`); `loadStoredState()` compares it to `golfmap:deployversion` and clears saved trips on any mismatch or first-ever load, fails closed if `APP_VERSION` is unavailable. Beta panel copy updated. Owner tested all 4 scenarios (mismatch clears / match keeps / first-load clears / fail-closed) and merged to `main` (`golf-127-132-sa-fees`, `5f0878a`, 2026-09-13).
- GOLF-129 / GOLF-35 Phase A / GOLF-102 Part 1 — beta launch readiness: `functions/_middleware.js` Basic Auth gate (Preview-only via `DEV_PASSWORD`, left unset/dormant — owner decided link-only + noindex is enough for this beta), `_headers`/`robots.txt` noindex, Worker CORS allowlist (`golf-map.pages.dev` + previews + localhost, else `null`), "Beta" badge + 4-point limitations panel in the Plan/Build navbar. Testers get the static production URL directly. (2026-09-13)
- GOLF-105 — basemap off the OSM public tile server (R-8) onto Esri's keyless `arcgisonline` raster tiles on both Leaflet maps; shared `esriBaseLayers()` factory + `L.control.layers` toggle: **Default** (Esri World Street Map) / **Satellite** (Imagery Hybrid); per-layer attribution swap; R-8 downgraded. True OSM-style (vector) punted to GOLF-106. (merged `main` `8490f33`, 2026-09-10)
- GOLF-120 popup fee-display redesign — single "Green fee" box when wd==we else two boxes; prominent amount; small "Confirmed" / italic "Estimate" sub-label; `.fee-pill` dropped; `docs/project/GOLF-120-fee-testing-plan.md` added for number-accuracy verification (merged `main` 2026-09-10)
- GOLF-125 — country pill now redraws the course-pin layer for the picked nation on **every** viewport (web included) + centres the camera on it. Round 1 (mobile-only, `js/explore.js`) missed that the visible pills are `#tb-nation-pills` in `js/trip-ui.js`, whose handler never called `render()`; round 2 (`golf-125-126-web`, merged `main` 2026-09-10) fixes that handler. CLOSED. (BUGS.md)
- GOLF-126 — wishlisting a course no longer stacks two `tripLayer` markers. Round 1 suppressed the background cluster pin; round 2 (`golf-125-126-web`, merged `main` 2026-09-10) stops `tripShow()` also drawing the plain flag pin for an anchor course that's also a stop (`anchorRingOnly`) — leaves the gold ring + the numbered trip marker. CLOSED. Residual marker-style / workflow polish → **GOLF-127**. (BUGS.md)
- GOLF-120 Phase 2 — `feeV2` green-fee schema live on `main`: reader + "highest applicable" derivation in `js/trip-geo.js` (tier 1 ahead of v1 `fee` / `wd`-`we`), `scripts/test_fee_v2.js`; headline (`£X` / `£X–£Y` / `Up to £X` / `from £X` / `POA`) + confidence pill wired through popup / tooltip / Explore / Costs / itinerary; mandatory-buggy own Costs line (owner Q6); 126-course batch-1 merged into all 5 `data/*.js` additively; `test_data.js` + `SCHEMA.md` updated (2026-09-10)
- GOLF-120 Phase 1 — green-fee schema v2 locked: proposal (`GOLF-120-schema-v2-proposal.md`) + batch-1 research for 126 top-ranked courses (`GOLF-120-fees-batch1.json`, 9 Haiku agents); owner answered all open questions 2026-09-10 (2026-09-10)

- GOLF-108 — course-pin layer no longer suppressed once a trip exists: Discover always shows it; Itinerary gets a bounded "Nearby courses" toggle (default ON); `bgCoursePins` pane keeps the route on top (branch `golf-108-109-course-pin-visibility` 2026-09-09)
- GOLF-109 — multi-course venues: all constituent courses now pinned (fell out of GOLF-108; `tripShow()` also switched to the shared jittered-coord lookup)
- GOLF-121a — South Africa full DotGolf pass: 99 → 421 courses, OSM-corrected coords, 3 new regions, Randpark split (merged `main` 2026-09-09)
- GOLF-121d — SA map ringfenced to the top 100: `zaRanked:1` on 107 union-ranked courses, `courseShownOnMap()` gate, `t100.za` re-derived 1–107 (branch `golf-108-109-course-pin-visibility`, 2026-09-09)
- GOLF-121e research — Australia Top 100 pulled & reviewed (`GOLF-121-australia-research.md`); build parked with a pickup note (`HANDOVER-GOLF-121e-australia.md`), 2026-09-09
- GOLF-122 — stale-after-deploy fixed: `sw.js` network-first for navigations, cache-first for the rest
- GOLF-117 — ferry-leg research spike: ORS crosses ferries already (inconsistently), flag-only v1 recommended → GOLF-118
- GOLF-114 — nation-pill row: equal-width grid keyed off `NATIONS.length` + header spacing
- GOLF-110 — nearest-railway-station feature hidden behind `RAIL_FEATURE` flag (dormant)
- GOLF-113 — "Discover by Region" list now scoped to the selected nation
- GOLF-115 — long trip names no longer clipped (ellipsis + title; shared view shows full name)
- GOLF-97 — banded green-fee schema (`fee:{…}`) — live
- GOLF-96 — "Add a stay" map hotel picker — COMPLETE: Worker `handleHotels` deployed & verified live (returns `pois`), 2026-09-10
- GOLF-95 — prompt-based reorder suggestion (no silent reorder)
- GOLF-94 — nation pills moved to top; auto 1-day-per-course scheduling
- GOLF-83/83b/83c — heritage POIs wiki-first + wineries
- Explore mode retired; pin redesign; Nearby/place-search consolidation
- GOLF-85 — Scotland Top 100 broadening; Ireland/SA Top 100 broadening
- GOLF-70/71/72–76 — script modularisation + design-system pass
