# Bug list — minor / polish

_Small defects and visual regressions that don't warrant a full backlog
discovery cycle. Continue the `GOLF-nnn` sequence. Promote to `BACKLOG.md`
if one turns out to be non-trivial. Newest at the bottom of each priority
group._

| ID | Area | Bug | Priority | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOLF-123 | Course card / popup | **"FROM HOME 5961 mi, as the crow flies"** straight-line distance is shown on every course card and needs to be removed. It's a raw great-circle distance from a hard-coded home location — meaningless to the user, and wrong-looking (thousands of miles). Remove the row/label entirely from the course card + popup render. If a "from home" figure is ever wanted it should be driving distance and opt-in — out of scope here, just delete the crow-flies one. | P2 | FIXED | Removed the row from both render sites — the `<dt>From home</dt>` line in `popupHTML()` (js/map.js) and the `X mi from home` span in the course card (js/explore.js). `distMiles()` (js/util.js) had no other caller and is deleted; `distOut()` stays (the "distance" sort still uses it). No "from home" figure now; a real driving-distance one would be a separate opt-in feature. |
| GOLF-124 | Map pins | New golf-ball-on-a-tee pins (GOLF-111) render correctly when you first focus a location, but as soon as you **add a course to the trip** the affected markers revert to the old default pin look. They should keep the new pin style in every state (default / in-trip / played / want). Likely the `render()` `setIcon` path after a trip mutation rebuilds the marker with the pre-GOLF-111 icon instead of `pinFor()` / `golfPinSVG()`. | P2 | FIXED | Root cause wasn't `setIcon` — once the trip is non-empty `render()` (js/explore.js) deliberately skips the whole main marker layer to declutter, and the discovery-candidate dots that replace it (`tripShow()`, js/trip-geo.js) were bare `L.circleMarker`s. Fix: `tripShow()` now draws each candidate with `pinFor(i)` — same golf-ball pin, size step, gold halo and in-trip/played/want tee tint as the Explore layer; out-of-radius candidates just fade to 0.5 opacity; the anchor keeps a gold ring behind its pin. Numbered itinerary route stops (⛳ day badges) unchanged. |
| GOLF-125 | Map pins | On a narrow / mobile viewport the map is `display:none` until the list↔map toggle (`showMobileMap()`). Picking a **country pill** ran `fitBounds()` + added the course markers to that hidden zero-size map, so the pins only appeared once a later town/course search happened to reveal the map. Selecting a country should show the pins straight away. | P1 | CLOSED | Nation-pill handler (`js/explore.js`) now calls `showMobileMap()` when a country is picked (desktop >900px ignores it) and defers the `fitBounds()` to a `setTimeout(…,0)` so it runs after `showMobileMap()`'s `invalidateSize()`, giving a correct country-level zoom instead of a stale max-zoom. Desktop unchanged (pins already showed there). **Reopened & re-fixed 2026-09-10** (`golf-125-126-web`): the first fix only touched the legacy `#nation-pills` handler in `js/explore.js`, which is a 0×0 hidden element — the *visible* pills are `#tb-nation-pills` in `js/trip-ui.js`, whose handler called `renderTripBuilder()+tbDrawMap()` but never `render()`, so on **any** viewport (web included) picking Ireland / South Africa moved the camera but never rebuilt the background course-pin layer for the new nation filter. That handler now calls `render()` (which does `renderTripBuilder()+tbDrawMap()` itself) + `state.sort='rank'` for parity. |
| GOLF-126 | Map pins | Adding a course to the wishlist left its **background cluster pin on the map** as well as drawing the trip marker — two pins for one course (the "old pin image" the report mentions is the numbered trip-stop / anchor marker sitting on top of the untouched bg pin). | P2 | CLOSED | `js/trip-geo.js` tracks the course indices `tbDrawMap()` puts on `tripLayer` in a `tripDrawnCourses` Set (populated by `tripShow()` for candidates + anchor and by `tripShowOrdered()` for stops, cleared in `tripClear()`). `render()` (js/explore.js) skips those indices when filling the background cluster layer, so a wishlisted / discovery / anchor course is drawn once. Removing it from the trip restores its bg pin on the next `render()`. **Reopened & re-fixed 2026-09-10** (`golf-125-126-web`): the bg-pin suppression worked, but the duplicate was still visible because it was *two `tripLayer` markers* — wishlisting one course makes it both the discovery **anchor** (drawn as a plain flag pin by `tripShow()`) and a day-1 **stop** (drawn as a numbered trip marker by `tripShowOrdered()`), stacked. `tbDrawMap()` now passes `anchorRingOnly` to `tripShow()` when the anchor is also a stop → only the gold ring + numbered marker are drawn for it. |

| GOLF-141 | Map pins | Hotel/POI itinerary stops showed a stray hollow numbered ring ("D3·2" style) on top of their own 🏨/📍 marker. | P2 | FIXED | `tripShowOrdered()` (`js/trip-route.js`) drew the hollow numbered-ring marker for every non-course stop type (place/hotel/poi) — a 2026-09-02 fix that widened it from `place`-only to avoid a crash when hotel/poi stops hit the course popup branch. But hotel/poi items already get their own emoji marker from `tbDrawTripItems()`, called in the same `tbDrawMap()` pass, so they ended up double-marked. The ring is now drawn only for `stop.type==='place'` (the day-level city anchor, which has no marker elsewhere); hotel/poi points still feed `fitBounds`/the route line as before. Verified via a synthetic day with a golf + hotel + poi stop: only the day-anchor ring remains, hotel/poi show one marker each. |

## Recently fixed (one line each — prune quarterly)

- **GOLF-123** (2026-09-08) — removed the meaningless "X mi from home, as the crow flies" row from the course card and popup (raw great-circle distance from a hard-coded home point).
- **GOLF-124** (2026-09-08) — nearby-course map pins kept the GOLF-111 golf-ball style after a course is added to the trip (`tripShow()` now uses `pinFor()` instead of plain circle dots).
- **GOLF-125** (2026-09-10) — course pins now appear immediately when a country pill is picked on mobile (nation-pill handler calls `showMobileMap()` + defers `fitBounds`), not only after a later search.
- **GOLF-125 (round 2)** (2026-09-10, `golf-125-126-web`) — the visible pane pills (`#tb-nation-pills`, `js/trip-ui.js`) never called `render()`, so on web too, picking Ireland / South Africa moved the camera but drew no pins for the new nation. Handler now calls `render()`.
- **GOLF-126** (2026-09-10) — no more duplicate pin when a course is wishlisted: `render()` skips courses already drawn on `tripLayer` (`tripDrawnCourses`) when filling the background cluster layer.
- **GOLF-126 (round 2)** (2026-09-10, `golf-125-126-web`) — wishlisting a course made it both the discovery anchor (plain flag pin) and a stop (numbered marker), stacked; `tbDrawMap()` now tells `tripShow()` to draw only the ring for an anchor that's also a stop.
- **GOLF-127** (2026-09-13, `golf-127-132-sa-fees`, merged `main` `5f0878a`) — redundant gold anchor-ring removed: `tripShow()` (`js/trip-geo.js`) now only draws the ring when `!pinStateTint(anchor)`, since a wishlisted/in-trip anchor's pin is already yellow (GOLF-130). The browsing-before-wishlisting case (anchor not yet in the trip, pin still white) keeps the ring as its only "this is your anchor" cue. Owner tested and merged.
| GOLF-168 | Costs tab → Stays breakdown | **A multi-night stay's explanatory line contradicts the figure next to it by 3x.** Expanding Stays on a 2-night hotel at £95pppn for 3 guests reads `The Castle Hotel (£285/night × 2 nights) × 3 people — £570`. The £285 **already is** the three people (£95 × 3), so the line states £285 × 2 × 3 = £1710 against a correct total of £570. **Every total in the app is right** — this is purely the explanatory line, which is precisely the thing that exists to make a visitor trust the number. Cause (`js/trip-ui.js:355-362`, verified): when a second night joins a stay group the row's `label` is rewritten to the `(£X/night × N nights)` form using `row.amount/row._nights`, but the `tag` set at creation on line 361 (`× ${det.guests} people`) is never cleared, so both survive and read as one multiplication chain. Single-night stays don't show it because label and tag agree there. Fix is to drop or rephrase the `tag` once `_nights > 1`. | P3 | **CANCELLED 2026-09-20 — owner's call, working as designed** | **No number is wrong and none ever was.** The per-person-per-night model works exactly as the owner describes: £100 pppn × 4 people = £400/night, and a 2-night stay at £95 pppn for 3 totals £570 correctly. The only thing raised was the **muted trailing `× 3 people` tag** (rendered via `<span class="wt">` at `js/trip-ui.js:474`) surviving on the label after it is rewritten to the `/night × N nights` form — so the cell reads `(£285/night × 2 nights) × 3 people` where the £285 already contains the three people. Owner: *"I think it was working fine. I haven't noticed it up until now."* Cosmetic, never observed in practice, closed on that basis. **If anyone re-notices it:** this is the whole defect, the fix is to clear `tag` once `_nights > 1` (`js/trip-ui.js:355-362`), and it does not indicate a costing error. |
| GOLF-177 | Course popup → club contact | **Two Top 100 courses show an artisans' section personal mobile as the club phone.** `data/courses-top100.js` ships `clubInfo.phone:"07946 158647"` on **Ashridge** and `clubInfo.phone:"07808835932"` on **Royal Cinque Ports**. Both are mobile numbers belonging to the *artisans' club* that shares the course, not the club office (Ashridge is `01442 842244`, Royal Cinque Ports `01304 374007`). Visible in the course popup, so a visitor ringing to book calls a private individual. | P2 | OPEN — raised 2026-09-22 | **Cause is diagnosed, not guessed.** Found by GOLF-162's behaviour-preservation diff: the old `GetClubsByName` + `difflib` lookup matched "Ashridge" to *Ashridge Artisans* and "Royal Cinque Ports" to *Royal Cinque Ports Artisans*, and `merge_club_details.py` then wrote the artisans' contact details onto the main club's record. Coordinates were unaffected (same site), so only `clubInfo` is wrong — the map has always been right, which is why this went unnoticed. GOLF-162's rewrite fixes the *lookup*; it deliberately does not touch `data/courses-*.js`, so the shipped records still carry the bad numbers. **This is a `data/courses-*.js` edit — DEC-024 territory**, patched in place by `id` per GOLF-163, never by rebuilding the array. Verify each replacement number against the club's own site before writing it; do not copy the numbers above on trust. **Scope is settled, checked 2026-09-22: `phone` is the entire blast radius, and it is exactly these two records.** `merge_club_details.py` only ever writes `phone`, `membership`, `teeBooking` and `blurb` — it has no website field, so `site:` cannot have been contaminated by this path, and both read correctly (`ashridgegolfclub.ltd.uk`, `royalcinqueports.com`). Neither record carries `membership`, `teeBooking` or `blurb` at all. The other names GOLF-162's rewrite now sends to human review were also checked: `notts-hollinwell-8348`, `royal-st-george-s-e753`, `gailes-links-7c6c` and `conwy-caernarvonshire-7597` all hold the **right** club's number, and `swinley-forest-28a2`, `castle-stuart-2476` and `newport-0ad6` have no `clubInfo` at all. So this is a two-field fix, not an audit. |
